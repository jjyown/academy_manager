# 출석관리앱 작업 계획서

- 문서 기준일: 2026-03-15
## 프로젝트 목표
- 학생/수업 기준으로 출석 상태를 정확히 기록하고 조회한다.
- 교사 권한으로만 수정 가능하도록 접근 제어를 적용한다.
- 변경 이력을 남겨 추적 가능한 운영 상태를 유지한다.

## 작업 운영 원칙
- 작업 시작 즉시 `python qa-artifacts/sync_doc_dates.py`를 실행해 `문서 기준일`을 당일로 자동 동기화한다.
- 작업 시작 시 `docs/plan.md`, `docs/context.md`, `docs/checklist.md`의 `문서 기준일`을 시스템 오늘 날짜로 먼저 갱신한다.
- 날짜는 작업일 기준으로 매번 갱신한다(예: 오늘 2026-03-06 저장, 다음날 작업 시 2026-03-07로 재저장).
- 작업 1개가 끝날 때마다 `docs/plan.md`, `docs/context.md`, `docs/checklist.md`를 즉시 동기화한다.
- 문서 업데이트가 끝나기 전에는 해당 작업을 완료로 간주하지 않는다.
- **강제 규칙**: 최종 응답 전 `문서 기준일`이 3개 문서에서 한 글자라도 다르면 작업 완료 금지(즉시 수정 후 재검증).
- 롤플레잉/전문가 의견 요청이 있으면 관련 전문가 의견을 답변에 포함하고, 의사결정 반영 시 문서 3종에도 기록한다.
- 사용자가 전문가 구성을 지정하지 않아도 요청 맥락에 맞는 전문가를 자동 선택해 의견을 포함한다.

## 현재 스프린트 목표
- [ ] 출석 입력/조회 기본 흐름 안정화
- [ ] 결과 화면(`grading/index.html`) 동작 및 UX 점검
- [x] 결과 API(`grading-server/routers/results.py`) 검증 강화

## 작업 트랙 분리 (중요)
- 브랜드/디자인 트랙: 간판 기준 리디자인 반영 작업(메인/결과/서브 화면)은 1차 완료 상태
- 재채점 안정화/검증 트랙: 운영 안정성 기준으로 PASS 마감, 후속 모니터링만 유지
- 현재 진행 트랙: 학생 관리 + 수납 관리 대규모 업데이트(세무/교육/UX 통합 설계 및 구현)
- 즉, 지금 작업은 디자인 개편의 후속이 아니라 수납 운영 실무성/세무 편의 중심 기능 고도화 작업

## 인계 요약 (학생관리 전환 전)
- 수납/비용 트랙은 핵심 흐름 구현이 1차 완료:
  - 수납/비용 탭 분리, 일/월마감 요약, 대사 차이, CSV 확장
  - 인건비 소득유형 분기(비율제/월급제), 자동 계산/필수 검증
  - 월별 세무/노무/회계 체크리스트 + 조건부 활성화
  - AI 수납 증빙 추출 + Drive 자동 저장(`수납증빙/학원명또는owner/YYYY/MM/DD/항목`)
- 학생관리로 전환 시 우선 구현:
  1) 학생 기본정보 정규화(재원상태/시작·종료일/보호자 연락처)
  2) 학생 상세 화면에 수납·비용 요약 연결(학생 기준 타임라인)
  3) 학생 기준 누락 점검(미수금/미확인입금/증빙 미연결) 배지 추가

### 학생관리 1차 구현 반영(등록폼 UX)
- [x] 학생 등록/수정 저장 시 필수 검증 강화(이름, 등록일, 연락처 1개 이상)
- [x] 학생/학부모 연락처 형식 검증 추가(휴대전화 형식)
- [x] 동일 학생 의심 중복 차단(이름+학년+연락처 일치 시 저장 중단)
- [x] 등록폼 안내 문구 보강(필수 입력 기준을 폼 내 명시)
- [x] 다음 단계(학생관리): 재원상태/시작·종료일/보호자 연락처를 중심으로 학생 스키마 확장 + 상세 화면 연결(등록폼/저장까지 완료)

### 학생관리 2차 구현 반영(일정삭제 고려 + 테스트 점수)
- [x] 일정 일괄삭제/기간삭제 모달에 삭제 예정 건수 미리보기 추가(삭제 전 영향 확인)
- [x] 학생 이력 모달에 테스트 점수 입력(시험명/시험일/점수/만점) 기능 추가
- [x] 월별 점수 변화 추이 UI 추가(평균/변화/건수 + 막대 추이 + 항목별 삭제)
- [x] 다음 단계(학생관리): 테스트 점수 데이터를 Supabase 동기화 구조로 확장(다기기 공유) - 코드 반영 완료

### 학생관리 3차 구현 반영(점수 원격 동기화)
- [x] 테스트 점수 Supabase 연동 함수 추가(`database.js`)
- [x] 점수 저장/조회/삭제 로직을 원격 우선 + 로컬 폴백 구조로 전환(`script.js`)
- [x] 점수 동기화용 SQL/RLS 스크립트 추가(`STUDENT_TEST_SCORE_SETUP.sql`)
- [ ] 운영 Supabase SQL Editor에서 `STUDENT_TEST_SCORE_SETUP.sql` 실제 실행

### 학생관리 4차 구현 반영(학생 스키마 확장)
- [x] 학생 등록/수정 폼에 `재원 상태`, `시작일`, `종료일`, `보호자 이름` 입력 필드 추가
- [x] 저장 검증 강화: 시작일 필수, 휴원/퇴원 시 종료일 필수, 종료일<시작일 차단
- [x] 학생 저장 시 확장 필드(`guardian_name`, `enrollment_start_date`, `enrollment_end_date`)를 DB 동기화 대상으로 포함
- [x] DB 미마이그레이션 환경 폴백: 확장 컬럼 오류 시 메모 메타로 임시 저장 후 기능 계속 사용 가능
- [x] 학생 스키마 확장 SQL 추가(`STUDENT_SCHEMA_UPDATE.sql`) + `SUPABASE_COMPLETE_SETUP.sql` 동기화
- [ ] 운영 Supabase SQL Editor에서 `STUDENT_SCHEMA_UPDATE.sql` 실제 실행

### 학생관리 5차 구현 반영(학생 상세 통합 뷰)
- [x] 학생 이력 모달 상단에 통합 요약 카드 추가(출석/숙제/수납/테스트 점수)
- [x] 월 기준 숙제 집계 연동(예정일/제출건/미제출일)
- [x] 월 기준 수납 집계 연동(수납액/청구액/미수금, 원격 우선 + 로컬 폴백)
- [x] 월 기준 테스트 점수 집계 연동(평균%/응시건수)
- [x] 기록이 없는 달에도 통합 요약/평가/점수 섹션을 표시하도록 이력 모달 흐름 보강

### 학생관리 6차 구현 반영(일정 삭제 UX 보강)
- [x] 일괄 삭제/기간 삭제 모달에 삭제 영향 경고 문구 영역 추가
- [x] 경고 문구를 실시간 계산으로 연결(삭제 범위/학생 수/삭제 건수/월 범위)
- [x] 삭제 확인창 메시지 고도화(기간, 삭제 예정 건수, 다월 범위, 복구 불가 안내)
- [x] 다크 테마/모바일에서 경고 문구 가독성 스타일 보강

### 학생관리 7차 구현 반영(출석 주체 구분)
- [x] 수업 관리 모달에 출석 방식 배지 추가(학생 QR / 선생님 체크 / 미확인)
- [x] 선생님 수동 출석 처리 시 `check_in_time` 자동 기록 보강(수동 체크 식별 가능)
- [x] 월별 출석 이력 상세 배지 문구를 `학생 QR`/`선생님 체크`로 명확화

### 학생관리 8차 구현 반영(QR 긴급출석 1차)
- [x] 일정 미등록 상태에서 QR 스캔 시 `임시출석` 자동 처리(출석 흐름 유지)
- [x] 일정 없음 분기에서 기존 처리 기록을 먼저 확인해 중복 스캔을 `이미 처리`로 판정
- [x] 임시출석 토스트 추가(`임시출석 처리 · 확정 대기`)로 운영자가 후속 조치 필요성을 인지
- [x] 임시출석 확정 워크플로우(확정 대기함/수동 확정/사유 기록) 연결

### 학생관리 9차 구현 반영(임시출석 확정 워크플로우 1차)
- [x] QR 출석 관리 모달에 `임시출석 확정 대기함` UI 추가
- [x] 최근 임시출석 건 조회/목록화(확정되지 않은 건만 필터링)
- [x] 건별 확정 버튼 + 사유 입력 저장(`qr_judgment` 갱신 + 메모 확정 로그)
- [x] 확정 완료 후 대기함 즉시 갱신(중복 확정 방지)

### 학생관리 10차 구현 반영(임시출석 확정 워크플로우 2차)
- [x] 임시출석 확정 시 `확정자(교사명)`를 `qr_judgment`/`memo` 로그에 함께 기록
- [x] 임시출석 대기함 조회 기간 필터(최근 7일/14일/30일) 추가
- [x] 대기함 다중 선택 + 일괄 확정 기능 추가(공통 사유 또는 행별 사유 사용)
- [x] 일괄 확정 후 목록/요약 즉시 갱신 및 선택 상태 초기화(중복 확정 방지)

### 학생관리 11차 구현 반영(일정 누락 사전 예방 1차)
- [x] QR 출석 관리 모달의 오늘 요약 영역에 `일정 누락 경고` + `오늘 일정 빠른 등록` CTA 추가
- [x] QR 스캔 진입 전 당일 일정 여부를 확인하고, 일정이 없으면 경고 확인창 표시
- [x] 경고 확인창에서 `오늘 일정 등록` 선택 시 일정등록 모달로 즉시 이동(오늘 날짜 + 미등록 학생 자동 선택)
- [x] 일정 없이 스캔을 강행하는 경우 경고 토스트로 임시출석 후속확인 필요성을 재안내

### 학생관리 12차 구현 반영(학생 상세 통합 화면 액션화 1차)
- [x] 학생 이력 모달 통합 요약 카드에 도메인별 즉시 액션 버튼 추가(출석/평가/수납/테스트)
- [x] `출석 처리` 액션: 오늘(또는 해당월 최근 일정) 출석 모달로 즉시 이동, 일정 미등록 시 일정등록 모달 자동 제안
- [x] `평가/메모 작성` 액션: 종합평가 입력 영역으로 즉시 스크롤/포커스
- [x] `수납 원장 열기` 액션: 해당 학생·월 기준 원장 모달 바로 열기
- [x] `점수 입력` 액션: 테스트 점수 섹션으로 즉시 이동 후 입력 포커스

### 학생관리 13차 구현 반영(테스트 점수 원격 동기화 운영 검증 1차)
- [x] 테스트 점수 섹션에 동기화 상태 배지 추가(원격 정상/로컬 폴백/미확인)
- [x] 저장/조회/삭제 경로에서 원격 성공/실패 상태를 즉시 반영하도록 동기화 상태 로직 연결
- [x] `동기화 점검` 버튼 추가: 임시 진단 레코드 원격 저장→조회 검증→삭제까지 스모크 테스트 자동 수행
- [x] 점검 실패 시 로컬 폴백 안내 문구/토스트로 운영자 대응 포인트를 즉시 노출

### 학생관리 14차 구현 반영(출석인증 긴급 보강: 전화번호 뒷자리)
- [x] QR 스캔 페이지를 태블릿 가로모드 기준 2열로 확장(좌: 카메라, 우: 전화번호 인증 패널)
- [x] 전화번호 뒷자리 4자리 입력으로 학생/보호자 연락처 매칭 후 출석 처리 연결
- [x] 동일 뒷자리 다중 매칭 시 학생 선택 UI를 제공해 오인증 방지
- [x] 기존 중복 처리 보호를 유지해 이미 처리된 출석은 `already_processed`로 판정(중복 임시출석 방지)
- [x] 전화번호 인증 출석도 기존 일정 판정/임시출석/확정 대기 흐름과 동일하게 동작하도록 통합

### 학생관리 15차 구현 반영(일정생성 모달 UI 버그 수정)
- [x] 일정생성 모달의 선택 학생 칩에서 `x`가 2개 보이던 중복 표시 버그 수정
- [x] 원인 정리: 칩 텍스트(`script.js`)의 `×`와 CSS `::after`의 `×`가 중복 렌더링됨
- [x] 조치: `.schedule-selected-chip::after` 제거로 단일 `×`만 노출되도록 정리
- [x] 영향 범위: 일정생성/기간삭제 모달의 동일 칩 UI가 함께 일관되게 정리됨

### 학생관리 16차 구현 반영(운영 SQL 실행 점검 패키지)
- [x] 테스트 점수 동기화 운영 검증 쿼리 파일 추가(`STUDENT_TEST_SCORE_VERIFY.sql`)
- [x] 학생 스키마 확장 운영 검증 쿼리 파일 추가(`STUDENT_SCHEMA_VERIFY.sql`)
- [x] SQL 실행 전/후 확인 포인트를 쿼리 중심으로 표준화(테이블/인덱스/RLS/정책/제약)
- [ ] 운영 Supabase SQL Editor에서 실제 실행 및 결과 확인(수동 작업 필요)

### 학생관리 17차 구현 반영(오늘 운영 대시보드 1차)
- [x] QR 출석 관리 모달 상단에 `오늘 운영 대시보드` 추가(일정 누락/임시출석 미확정/다음 수업)
- [x] 즉시조치 버튼 추가(`바로 스캔`, `누락 일정 등록`, `임시확정 보기`)
- [x] 임시출석 대기함 렌더 시 대시보드 요약 자동 갱신 연결(확정 직후 카운트 반영)
- [x] 모바일/다크테마 가독성 스타일 보강(카드/버튼 레이아웃 반응형)
- [ ] 실사용 기반 문구/카드 우선순위 미세조정(현장 피드백 반영 2차)

### 학생관리 18차 구현 반영(출석체크 화면 탭 단순화 1차)
- [x] QR 스캔 페이지를 3탭 구조로 단순화(`QR 스캔` / `전화번호` / `수동체크`)
- [x] 탭 전환 시 화면/액션 컨트롤 동기화(카메라 액션 버튼 표시/숨김)
- [x] 비QR 탭 진입 시 카메라 스캐너 자동 중지, QR 탭 복귀 시 자동 재시작
- [x] 수동체크 탭에 운영 보조 요약(오늘 일정 등록/누락 수) + 즉시조치 버튼 제공
- [ ] 수동체크 탭에서 학생 단건 즉시 체크(탭 내 완결 처리) 2차 보강

### 학생관리 19차 구현 반영(태블릿 전체화면 듀얼 표시 1차)
- [x] 10인치 태블릿 가로 사용을 기준으로 `QR 스캔` 모드에서 카메라/전화번호 입력을 동시 표시
- [x] 화면 폭 조건(900~1400px)에서 자동 듀얼 모드 활성화, 그 외 구간은 기존 단일/탭 모드 유지
- [x] 방향 전환/해상도 변경 시 `resize` 이벤트로 듀얼 모드 재판정 및 레이아웃 즉시 동기화
- [x] 기존 출석 처리 로직(QR/번호인증 중복보호/임시출석 흐름)은 변경 없이 UI 레이어만 확장
- [ ] 10인치 실기기에서 세로↔가로 전환 시 버튼 터치 영역/폰트 크기 추가 미세조정

### 학생관리 20차 구현 반영(학생용 출석 화면 고정형 분리 1차)
- [x] 학생 화면에서 수동체크 UI(탭/버튼/패널) 완전 제거
- [x] QR 스캔 + 전화번호 인증 2패널 고정 구조로 단순화(탭 전환 없이 동시 사용)
- [x] 가로(landscape)에서는 좌우 분할, 세로(portrait)에서는 상하 배치로 자동 전환
- [x] 카메라는 페이지 진입 즉시 시작 유지(탭 상태 의존 제거)
- [x] 학생/교사 권한 분리 2차: 학생 화면에서 교사 기능 진입 자체를 PIN 레벨로 추가 고정

### 학생관리 21차 구현 반영(10인치 태블릿 터치 최적화 2차)
- [x] 상단 제어 버튼(전체화면/카메라전환/닫기)을 클래스 기반으로 정리하고 터치 영역 확대
- [x] 전화번호 입력/인증 버튼 최소 높이를 상향해 오터치 가능성 감소
- [x] 900~1400px 구간(10인치 중심) 전용 UI 스케일 업(폰트/간격/패널 여백) 적용
- [x] 듀얼 패널(좌우) 비율을 태블릿 가독성 기준으로 미세 보정
- [ ] 실기기 체감 기준으로 숫자패드 호출 후 가시 영역(키보드 오버레이) 미세조정

### 학생관리 22차 구현 반영(키보드 오버레이 대응 1차)
- [x] 전화번호 입력 포커스 시 `visualViewport` 기반 키보드 높이 감지 로직 추가
- [x] 키보드 오픈 상태에서 QR 화면 하단 여백(`--qr-keyboard-offset`) 자동 보정
- [x] 키보드 오픈 시 입력 패널 자동 스크롤 및 듀얼 레이아웃 단일열 전환으로 가림 최소화
- [x] QR 스캔 페이지 open/close 시 뷰포트 이벤트 바인딩/해제를 안전하게 관리
- [ ] 안드로이드 기기별 키보드(삼성/구글) 편차 기반 임계값(현재 120px) 실측 튜닝

### 학생관리 23차 구현 반영(운영 검증 SQL 치환 오류 방지)
- [x] `STUDENT_TEST_SCORE_VERIFY.sql`에서 `REPLACE_WITH_OWNER_UUID` 미치환 시에도 실행되도록 안전 파라미터 처리 추가
- [x] `STUDENT_SCHEMA_VERIFY.sql`도 동일 방식으로 보강(미치환 시 전체/최근 샘플 조회)
- [x] 운영 SQL Editor에서 즉시 실행 가능한 검증 쿼리 안정성 확보
- [ ] 필요 시 실제 owner UUID를 치환해 단일 원장(owner) 기준 결과만 필터 검증

### 학생관리 24차 구현 반영(스키마 검증 쿼리 컬럼 호환 보강)
- [x] `STUDENT_SCHEMA_VERIFY.sql`의 `updated_at` 의존 제거(컬럼 미존재 환경 호환)
- [x] 샘플 조회 정렬 기준을 `id desc`로 변경해 공통 스키마에서 실행 가능하도록 정리
- [x] Supabase SQL Editor 즉시 실행 시 `column "updated_at" does not exist` 오류 재발 방지

### 학생관리 25차 구현 반영(학생 목록 이력/점수 진입 버튼 보강)
- [x] 학생 목록 카드에 `이력` / `점수` 퀵 액션 버튼 추가
- [x] `이력` 버튼: 해당 학생 기준 `학생 이력` 모달 즉시 오픈
- [x] `점수` 버튼: 이력 모달 오픈 후 테스트 점수 섹션 자동 포커스
- [x] 모바일/다크테마에서 버튼 가독성/터치영역 스타일 보강
- [x] 기존 카드 탭(학생 정보 수정) 동선은 유지해 회귀 방지

### 학생관리 26차 구현 반영(재석확인 오탐 팝업 방지)
- [x] 재석확인 팝업 표시 전, 큐 항목의 `오늘 날짜 + 실제 남아있는 일정(start time)` 재검증 추가
- [x] 일정 이동/삭제로 무효화된 항목은 재석확인 큐에서 즉시 제거
- [x] 유효 항목이 0건이면 팝업 자체를 열지 않도록 처리
- [x] 학생이 일정을 옮긴 직후에도 "학생이 자리에 있나요?" 오탐 알림이 뜨지 않도록 안정화

### 학생관리 28차 예정(테스트 점수 앱 실데이터 운영 검증)
- [ ] 시나리오 A: 학생 1명 기준 `점수 저장 -> 재진입 조회 -> 삭제` PASS 확인
- [ ] 시나리오 B: 다른 학생/다른 월 1건 추가 저장 후 월별 조회 정합성 확인
- [ ] 동기화 상태 배지 확인(원격 정상/로컬 폴백) 및 실패 토스트 문구 확인
- [ ] 검증 완료 후 우선순위 5번을 완료 처리하고 체크리스트 PASS 기록
- 진행상태(2026-03-04): 브라우저 자동 검증을 `http://127.0.0.1:4173/`에서 시도했으나 `auth-page` 로그인 단계에서 차단되어 BLOCKED. 학생관리/점수 모달까지 진입하려면 운영 계정 로그인 정보가 필요함.

### 학생관리 29차 구현 반영(학생 화면 종료 PIN 잠금 1차)
- [x] QR 학생 출석 화면(`qr-scan-page`) 닫기 시 선생님 비밀번호 재인증(`showPrompt`)을 필수화
- [x] 현재 선택된 선생님(`current_teacher_id`)의 `pin_hash`로 해시 비교 검증 후에만 화면 종료 허용
- [x] 인증 실패/취소 시 화면 유지(학생이 닫기 버튼으로 운영 화면 이탈 불가)
- [x] 중복 클릭 보호(`qrClosePinCheckInProgress`)로 인증 모달 중첩을 방지

### 학생관리 30차 구현 반영(재석확인 팝업 닫힘 조건 보정)
- [x] `출석/지각/결석/보강` 처리 후 팝업 자동닫힘 기준을 "전체 대기 큐"에서 "현재 선생님 + 현재 처리 timeKey" 기준으로 수정
- [x] 다른 선생님/다른 시간대의 미처리 항목이 있어도, 현재 화면에서 처리 완료 시 팝업이 정상 종료되도록 보정
- [x] 처리 완료 항목 정리 시 현재 선생님의 `_done` 항목만 큐에서 제거하고, 타 선생님 대기건은 유지
- [x] 단건 처리 레거시 경로(`handleSingleAttendanceCheck`)에도 동일 닫힘/정리 기준 적용
- [x] 큐와 DOM 렌더 상태가 일시 불일치해도 팝업이 남지 않도록 UI 기준 보조 닫힘 판정(`unchecked checkbox` 없음)을 추가

### 학생관리 31차 구현 반영(재등록 일정 `이미 출석` 오탐 보정)
- [x] QR 출석 중복 판정에서 기존 기록 조회 시 `scheduled_time` 미일치 레코드 폴백을 비활성화(엄격 일치)하는 경로를 추가
- [x] `processAttendanceFromQR`의 중복 검사 호출을 `getAttendanceRecordByStudentAndDate(..., startTime, false)`로 변경
- [x] 효과: 일정 삭제 후 동일 시간 재등록 상황에서, 시간이 다른 과거 출석 레코드 때문에 `already_processed`가 뜨는 오탐을 방지

### 학생관리 32차 진행(하루 다중 일정 운영 점검 시나리오)
- [ ] 시나리오 A(정상 다중 일정): 같은 학생 `18:00/20:00` 등록 후 18:00 스캔 시 18:00만 처리되고 20:00은 재석확인 대기/후속 확인으로 남는지 검증
- [ ] 시나리오 B(재등록 오탐 방지): `20:00 등록 -> 출석 -> 삭제 -> 20:00 재등록 -> 재스캔`에서 `already_processed` 오탐 없이 정상 처리되는지 검증
- [ ] 시나리오 C(시간 경계): `20:00` 대비 `19:59/20:00/20:01` 스캔 각각의 상태(출석/출석/지각)와 재석확인 팝업 유무를 검증
- [ ] 시나리오 D(임시출석 혼합): 일정 미등록 임시출석 후 같은 날 정규 일정 추가 시 정규 일정 스캔이 임시출석 레코드와 충돌하지 않는지 검증
- [ ] 결과를 `docs/checklist.md`에 PASS/FAIL 및 재현 스텝(시간/학생/교사)으로 기록

### 학생관리 33차 구현 반영(전화번호 인증 `만료 QR` 오탐 보정)
- [x] 전화번호 인증 payload 생성 시 `qr_code_data` 우선 사용 + 미존재 시 임시 토큰 허용으로 진입 실패를 방지
- [x] `processAttendanceFromQR`에서 전화번호 인증(`phone_last4`) 경로는 QR 토큰 만료 검증(DB/로컬 토큰 비교)을 스킵하도록 분리
- [x] 효과: 일정 재등록 후 전화번호 인증 시 `만료된 QR코드`가 뜨는 오탐을 방지
- [x] 참고: `8:01` 스캔 시 `지각` 판정은 현재 정책(시작시간 이후=지각) 기준 정상 동작

### 학생관리 33차 구현 반영(전화번호 인증 `만료` 오탐 보정)
- [x] 전화번호 인증 payload 생성 시 `student_code` 미존재/불일치 상황에서도 처리 가능하도록 토큰 fallback(`PHONE_AUTH_*`) 추가
- [x] `processAttendanceFromQR`에서 `phone_last4` 모드는 QR 토큰 만료 검증을 스킵하도록 분기
- [x] 효과: 스케줄 재등록/코드 재생성 이후에도 전화번호 인증에서 `만료된 QR코드` 오탐이 뜨지 않도록 안정화

### 학생관리 34차 구현 반영(출석 판정 그레이스 2분 도입)
- [x] 출석 판정 상수 `ATTENDANCE_GRACE_MINUTES = 2` 추가
- [x] QR 메인 판정(`processAttendanceFromQR`)에서 `수업 시작 후 2분`까지 `출석`으로 처리
- [x] 2분 초과~수업 종료 전은 `지각`, 수업 종료 후는 `결석` 유지
- [x] 보조 판정 함수(`determineAttendanceStatus`)도 동일 기준으로 동기화

### 학생관리 35차 구현 반영(운영 테스트 입력 템플릿 제공)
- [x] 32차 A~D 실기기 점검 결과를 1줄로 공유할 수 있는 복붙 템플릿을 `docs/checklist.md`에 추가
- [x] 운영자 보고 형식 통일로 PASS/FAIL 수집 속도 향상

### 학생관리 36차 구현 반영(무일정 스캔 결석/이미처리 오탐 보정)
- [x] `already_processed` 판정에서 `absent` 상태를 제외하고 `present/late/makeup`만 처리완료로 인정
- [x] QR 스캔 일정 조회를 현재 선택 선생님(`currentTeacherId`) 기준으로 우선 제한
- [x] DB 조회 실패 시 로컬 fallback에서도 현재 선생님 일정만 필터해 판정
- [x] 효과: 일정 미등록인데 `결석`으로 떨어지거나, 삭제 후 재스캔 시 `이미 처리`로 막히는 오탐 감소

### 학생관리 37차 구현 반영(시간표에 임시출석 가시화)
- [x] `renderDayEvents`에서 일정이 없는 학생의 임시출석(`attendance[date].emergency = present`)을 수집
- [x] 시간표 그리드 상단에 `임시출석 n건` 배너를 노출해 학생명/학년을 즉시 확인 가능하게 보강
- [x] 효과: 임시출석 처리 후 시간표가 비어 보이는 혼선을 줄이고 운영자가 누가 임시처리됐는지 즉시 파악 가능

### 학생관리 38차 구현 반영(3/5 일정 보유 시 3/4 무일정 `이미처리` 오탐 보정)
- [x] 무일정 분기의 기존 기록 조회를 `currentTeacherId` 기준으로 제한(타 선생님 기록 혼입 방지)
- [x] 무일정 중복 판정 대상을 "임시출석 기록"(`scheduled_time null` 또는 `임시출석` 로그 포함)으로 축소
- [x] 효과: 다음날 일정만 있는 학생을 당일 스캔할 때 `already_processed` 오탐을 줄이고 임시출석 처리 정상화

### 학생관리 39차 구현 반영(무일정 `이미처리` 재오탐 추가 보정)
- [x] 무일정 분기의 중복조회 쿼리를 `scheduled_time IS NULL`(임시출석 전용)로 고정
- [x] 무일정 분기 조회에 `owner_user_id` + `currentTeacherId` 필터를 함께 적용해 타 원장/타교사 기록 혼입 차단
- [x] 공통 조회 함수 `getAttendanceRecordByStudentAndDate`에도 `owner_user_id` 필터를 추가해 전역 조회 정합성 강화
- [x] 효과: 3/5 일정만 있는 학생을 3/4에 스캔/번호인증할 때 `already_processed` 재오탐 가능성을 추가 축소

### 학생관리 40차 구현 반영(무일정 스캔 정책 전환: 미처리 + 자동일정)
- [x] 무일정 QR/번호인증 시 출석 레코드를 `미처리(status=none)`로 저장하고 QR 스캔 시각을 기록
- [x] 무일정 스캔 시 현재 시각(`HH:MM`) 기준으로 해당 학생 일정 1건을 자동 생성(로컬+DB 저장 시도)
- [x] 로컬 출석 캐시는 `attendance[date][autoStartTime] = none`으로 즉시 동기화해 시간표 상태 배지에 반영
- [x] 무일정 스캔 토스트/사전 경고 문구를 `임시출석` 중심에서 `미처리+자동일정` 정책으로 변경
- [x] 효과: 일정 미등록 상황에서도 기록 누락 없이 미처리 큐를 유지하고, 시간표에서 즉시 후속조치 가능

### 학생관리 41차 구현 반영(전화번호 인증 패널 숫자패드 추가)
- [x] `qr-phone-last4-input` 하단에 3x4 소형 숫자패드(0~9, 초기화, 지우기) 추가
- [x] 키패드 입력 핸들러(`handlePhoneAuthKeypadKey`, `handlePhoneAuthKeypadAction`)를 추가해 터치 입력만으로 4자리 입력 가능
- [x] 10인치 구간(900~1400px)에서 숫자패드 버튼 크기/폰트를 확대해 오터치 감소
- [x] 효과: 태블릿 환경에서 가상키보드 의존도를 줄여 전화번호 인증 입력 속도/정확도 개선

### 학생관리 42차 구현 반영(숫자패드 확인 버튼 일체화)
- [x] 숫자패드 하단에 `키패드 확인` 버튼을 추가해 입력/인증 동선을 한 영역으로 통합
- [x] 기존 `handlePhoneAuthKeypadAction('submit')`와 연결해 숫자패드 터치만으로 인증 실행 가능
- [x] 10인치 구간에서 확인 버튼 높이/폰트를 확대해 한 손 조작 가독성 개선
- [x] 효과: 숫자 입력 후 상단 `인증` 버튼으로 이동하는 동선을 줄여 반복 입력 속도 향상

### 학생관리 43차 구현 반영(4자리 입력 즉시 자동 인증)
- [x] 전화번호 입력창 `oninput`을 `handlePhoneAuthInputChange`로 전환해 4자리 완성 시 자동 인증 트리거
- [x] 숫자패드 입력(`handlePhoneAuthKeypadKey`)도 4자리 완성 시 자동 인증이 실행되도록 연동
- [x] 인증 실행 중 중복 호출 방지를 위해 `phoneAuthSubmitting` 가드 추가
- [x] 효과: `입력 완료 → 즉시 인증` 흐름으로 전환되어 버튼 추가 조작 없이 출석 처리 가능

### 학생관리 44차 구현 반영(전화번호 인증 버튼 제거)
- [x] 상단 `인증` 버튼 제거(4자리 자동인증 흐름과 중복되는 수동 버튼 제거)
- [x] 하단 `키패드 확인` 버튼 제거(숫자 입력 완료 즉시 인증으로 대체)
- [x] 버튼 제거 후 입력창 폭/스타일을 단일 입력 구조로 정리
- [x] 효과: 학생 화면 조작 요소를 최소화해 오터치 가능성을 줄이고 입력 동선을 단순화

### 학생관리 45차 구현 반영(전화번호 인증 대상 제한: 학생 번호 전용)
- [x] 전화번호 뒷자리 매칭 대상을 `학생 연락처`로만 제한(보호자 연락처 매칭 제거)
- [x] 번호 미일치 안내 토스트 문구를 학생 연락처 기준으로 정정
- [x] 전화번호 인증 설명 문구를 학생 번호 전용 정책으로 동기화
- [x] 효과: 학생 본인 인증 정책을 명확히 해 보호자 번호 입력으로 인한 오인증 가능성을 차단

### 학생관리 46차 구현 반영(학생 화면 상단 제어 버튼 숨김 + 더블탭 전체화면)
- [x] QR 스캔 페이지 상단 `전체화면`/`카메라 전환` 버튼을 학생 화면에서 제거
- [x] 상단 제목 영역 더블탭(터치/더블클릭)으로 전체화면 토글 동작 추가
- [x] 닫기 버튼은 기존대로 선생님 PIN 인증 후 종료되도록 유지
- [x] 효과: 학생의 임의 탭으로 화면/카메라 상태가 바뀌는 오조작 위험을 줄이면서 선생님의 현장 조작 동선은 유지

### 학생관리 47차 구현 반영(모서리 3초 롱프레스 관리자 액션 + 카메라전환 PIN)
- [x] QR 스캔 페이지 모서리 롱프레스(3초) 시 `카메라 전환` 버튼을 12초간 표시
- [x] 롱프레스가 끝나기 전에 손을 떼면 버튼이 표시되지 않도록 취소 처리
- [x] `카메라 전환` 버튼 실행 시 선생님 PIN 인증을 매번 요구하도록 보강
- [x] 효과: 평소에는 학생이 카메라 제어 UI를 볼 수 없고, 선생님만 의도적으로 노출/인증 후 전환 가능

### 학생관리 48차 구현 반영(다중 시간대 이력 가시화 + 일정 겹침 판정 보정)
- [x] 전문가 합의(프론트엔드 UX + 데이터 무결성): "같은 날짜 다중 수업은 날짜 단일행으로 집계하면 운영 오판을 유발" + "duration 타입 혼합(string/number)은 겹침 계산 전 정규화 필요"
- [x] 출석 이력 모달 집계 키를 `날짜`에서 `날짜+수업시간` 슬롯으로 확장해 같은 날 2개 이상 수업(예: 19:00/22:00)이 각각 표시되도록 수정
- [x] 이력 상단 건수 표기를 `총 n일`에서 `총 n건`으로 변경해 다중 시간대 운영과 일치하도록 정리
- [x] 일정 겹침 판정(`checkScheduleOverlap`)에서 `duration` 문자열(예: `"60"`)을 숫자로 정규화해 19:00/20:00 인접 시간대가 오탐으로 겹침 처리되지 않도록 보정
- [ ] 실기기에서 `19:00 출석 후 22:00 추가/출석` 재현 시나리오와 `19:00↔20:00` 인접 시간대 등록 시나리오를 PASS로 확정

### 학생관리 49차 구현 반영(다교사 시간표 스코프 + 후속 알림 라우팅)
- [x] 시간표 상세 모달에 표시 범위 토글 추가(`내 학생만`/`전체 선생님`) + 힌트 문구 동기화
- [x] 시간표 렌더/일자 배지 집계를 스코프 기준으로 분기(`mine`: 현재 선생님, `all`: 전체 선생님)
- [x] 전체 보기에서 블록 키에 `teacherId`를 포함해 레이아웃 충돌을 방지하고, 담당 선생님 배지를 노출
- [x] QR/번호인증 출석 처리 시 당일 일정 조회를 전체 선생님 기준으로 확장해 후속 수업 큐를 정확히 생성
- [x] 재석확인 팝업의 `현재 선생님 필터`는 유지해 알림이 담당 선생님에게만 노출되도록 보장
- [ ] 실기기 회귀: `19:00 A / 22:00 B`에서 19시 처리 후 22시 알림이 B에게만 뜨는지 PASS 확인

### 학생관리 50차 구현 반영(메인 시간표 토글 상시노출 + 일정 수정 권한 강화)
- [x] 메인 월/주간 헤더에 `시간표 범위(내 학생만/전체 선생님)` 토글을 상시 배치해 일자 상세 모달 진입 없이도 범위 전환 가능하도록 보강
- [x] 수업 관리 모달에 `att-owner-teacher-id`를 연결해 일정 소유 선생님 기준으로 조회/수정/삭제 경로를 일관화
- [x] 권한 정책 적용: 일반 선생님은 본인 일정만 수정/삭제 가능, 관리자(admin)는 관리자 PIN 재인증 후 타 선생님 일정 수정/삭제 허용
- [x] 개인/공유 메모 안내 문구를 운영 용도 중심으로 재정의(개인=비공유, 공유=교사 인수인계)
- [ ] 실기기 회귀: 타 선생님 일정 진입 시 `변경/삭제`에서 권한 제한 및 관리자 PIN 예외가 정책대로 동작하는지 PASS 확인

### 학생관리 51차 구현 반영(전체 일정 공유 기본화)
- [x] 시간표 스코프 기본값을 `all`(전체 선생님)로 변경하고, 저장값 복원 fallback도 `all`로 통일
- [x] 선생님 전환/재로그인 시 저장된 스코프를 `all`로 재설정해 "등록된 일정 전체 공유" 정책을 우선 적용
- [x] 메인/일자상세의 스코프 선택 UI 기본 노출 순서를 `전체 선생님 -> 내 학생만`으로 정렬하고 기본 안내문구를 공유 정책에 맞게 수정
- [ ] 실기기 회귀: 로그인 직후 월간/일자상세 모두 전체 일정이 즉시 보이는지, 필요 시 `내 학생만` 전환이 정상 동작하는지 PASS 확인

### 학생관리 52차 구현 반영(시간표 선생님 라벨 UUID 노출 보정)
- [x] 선생님 이름 조회 fallback에서 `선생님(UUID)` 형태 원문 노출을 제거하고 사용자용 라벨(`선생님`)로 통일
- [x] 현재 로그인 선생님 ID와 일치할 때는 세션의 선생님 이름을 우선 사용하도록 보강
- [x] 효과: 시간표 블록(전체 선생님 보기)에서 UUID/내부식별자가 UI에 노출되지 않아 가독성과 운영 신뢰도를 개선
- [ ] 실기기 회귀: 전체 선생님 보기에서 선생님 라벨이 이름 또는 `선생님`으로만 보이고 UUID 문자열이 재노출되지 않는지 PASS 확인

### 학생관리 53차 구현 반영(담당 선생님 이름 매핑 보강: owner UUID 호환)
- [x] 선생님 이름 조회 시 `teacher.id` 미매칭이면 `teacher.owner_user_id`까지 추가 매핑해 과거/혼합 데이터 호환
- [x] `schedule.teacher_id`가 owner UUID로 저장된 케이스는 현재 세션 선생님 이름으로 2차 보정
- [x] 효과: 시간표 블록에서 `선생님` 일반 라벨로만 떨어지던 케이스를 줄이고 실제 담당 선생님 이름 노출률 개선
- [ ] 실기기 회귀: 기존 `선생님`으로 보이던 블록이 담당 선생님 이름으로 표시되는지 PASS 확인

### 학생관리 54차 구현 반영(다중 일정 중 1건 삭제 시 타 시간대 출석 상태 보존)
- [x] 단건 일정 삭제(`deleteSingleSchedule`)에서 `attendance[date]` 전체 삭제를 중단하고 `attendance[date][startTime]`만 제거하도록 보정
- [x] 메모(`records`)도 동일하게 날짜 전체 삭제 대신 해당 시간 슬롯만 제거하도록 동기화
- [x] 레거시 문자열 구조(시간 슬롯 미구분)에서는 기존 호환을 위해 날짜 단건 삭제로 처리
- [x] 효과: `19:00/22:00` 모두 처리된 상태에서 `22:00` 삭제 시 `19:00`이 `미처리`로 바뀌는 회귀를 방지
- [ ] 실기기 회귀: `19:00/22:00 처리 -> 22:00 삭제` 후 `19:00` 상태가 그대로 유지되는지, 상태 조합(출석/지각/결석/보강)별로 PASS 확인

### 학생관리 55차 구현 반영(묶음 일정 블록 담당 선생님 이름 복원 보강)
- [x] 묶음 블록 렌더 시 선생님 표시명 해석 함수를 추가하고 `teacher.id` 매칭 실패 시 학생 `teacher_id`/로컬 학생-선생님 매핑을 순차 fallback으로 사용
- [x] 그룹핑 과정에서 초기 표시명이 `선생님`일 때 후속 이벤트의 실제 선생님명이 있으면 그룹 라벨을 이름으로 승격
- [x] 효과: `23:30~01:10` 같은 묶음 일정에서 라벨이 `선생님`으로만 보이던 케이스를 담당 선생님 이름으로 최대한 복원
- [ ] 실기기 회귀: 전체 선생님 보기의 묶음 블록에서 담당 선생님명이 정상 노출되는지 PASS 확인

### 학생관리 56차 구현 반영(타 선생님 일정: 관리자 인증 + 완전 보기 전용)
- [x] 타 선생님 일정 클릭 시 관리자 비밀번호 팝업을 먼저 표시하고 인증 성공 시에만 수업 관리 모달 진입하도록 보강
- [x] 타 선생님 일정 모달은 `보기 전용`으로 고정(출석 상태 버튼/일정 변경/메모 저장/색상 적용/일정 삭제 비활성화)
- [x] 권한 정책 단순화: 타 선생님 일정의 수정/삭제는 관리자 포함 전면 차단(체크용 조회만 허용)
- [x] 효과: 교차 교사 일정 오수정 위험을 구조적으로 제거하고, 조회는 유지하되 편집 권한은 소유 교사로 한정
- [ ] 실기기 회귀: A교사가 B교사 일정 클릭 시 관리자 인증 후 조회만 가능하고, 어떤 편집 액션도 실행되지 않는지 PASS 확인

### 학생관리 57차 구현 반영(기간/일괄 삭제 경로 소유자 고정 점검)
- [x] 기간 삭제(`executeBulkDelete`)와 기간별 삭제(`executePeriodDelete`) 시작 시 선생님 컨텍스트 검증 가드를 추가
- [x] 삭제 확인문구에 `대상: 내가 등록한 일정만`을 명시해 권한 정책을 UI에 고정
- [x] 효과: 삭제 경로에서도 교차 교사 일정 삭제 오해를 줄이고, 소유자 전용 정책을 운영자에게 명확히 안내
- [ ] 실기기 회귀: 전체 선생님 보기 상태에서도 기간/일괄 삭제가 현재 선생님 일정에만 적용되는지 PASS 확인

### 학생관리 58차 구현 반영(상태 적용 범위 팝업 제거 + 담당 선생님 즉시 반영)
- [x] 수업 관리 모달의 상태 변경 시 `상태 적용 범위(현재/전체)` 팝업 호출을 제거
- [x] 출석 상태 저장 경로를 담당 선생님(현재 선생님) 단일 적용으로 고정
- [x] 출석 이력 모달의 상태 변경도 동일 정책(현재 선생님 일정만 반영)으로 통일
- [x] 효과: 일정 등록 후 담당 선생님이 수정하면 추가 선택 없이 즉시 반영되고, 타 선생님 일정에 확장 반영되는 혼선을 제거
- [ ] 실기기 회귀: 다교사 학생(예: 19시 A / 22시 B)에서 A가 상태 변경해도 B 일정은 영향받지 않는지 PASS 확인

### 학생관리 59차 구현 반영(묶음 블록 클릭 시 과도한 관리자 인증 팝업 보정)
- [x] 수업 관리 모달 진입 전 owner teacher id를 정규화(`schedule owner -> 학생 teacher_id -> 학생-선생님 매핑 -> 현재 선생님`)해 legacy 식별자 혼입을 흡수
- [x] owner 라벨이 일반값(`선생님`)일 때 관리자 인증 문구의 중복(`선생님 선생님`)을 제거
- [x] 효과: 같은 시간 묶음 일정에서 본인 담당 학생 클릭 시 불필요한 관리자 인증이 반복되는 현상을 완화
- [x] 실기기 회귀: 묶음 블록에서 본인 담당 학생 클릭은 바로 진입, 타교사 학생 클릭만 관리자 인증이 뜨는지 PASS 확인

### 학생관리 60차 구현 반영(묶음 규칙 보정: 교사별 분리 + 동일교사만 묶기)
- [x] 전체 보기(`all`)의 그룹 키를 단순 `teacherId` 대신 교사 정규화 키(`raw teacher_id -> 학생 teacher_id -> 학생-선생님 매핑`)로 재계산
- [x] 같은 시간대라도 교사 정규화 키가 다르면 별도 블록으로 분리, 동일 교사 키는 기존처럼 하나의 묶음 블록으로 유지
- [x] 효과: A/B 선생님 동시간대 일정이 한 블록으로 합쳐지지 않고, A선생님 동시간대 다건만 묶여 표시
- [ ] 실기기 회귀: 동시간대 A/B 분리 + A동시간 다건 묶음이 동시에 만족하는지 PASS 확인

### 학생관리 61차 구현 반영(교사 라벨 가시성 강화)
- [x] 시간표 블록의 `담당 선생님` 배지를 고대비(블루 그라데이션 + 라운드 캡슐 + 강조 그림자) 스타일로 강화
- [x] 배지 앞에 `담당` 프리픽스를 추가해 분리된 블록에서 교사 식별 의미를 즉시 전달
- [x] 효과: 동시간대 다교사 분리 화면에서 어떤 블록이 누구 일정인지 시인성을 향상
- [ ] 실기기 회귀: 태블릿/PC에서 배지 가독성(야간 테마 포함)과 블록 겹침 시 식별성이 충분한지 PASS 확인

### 학생관리 62차 구현 반영(교사 수 무관 배지 색상 자동화)
- [x] 교사 배지 색상을 고정 단일색에서 `교사 식별자 해시 기반 동적 색상`으로 전환
- [x] 교사 수가 늘어나도 별도 설정 없이 각 교사 배지가 안정적으로 다른 색으로 생성되도록 보강
- [x] CSS는 `--evt-teacher-*` 변수 기반으로 변경해 기본 스타일 유지 + 동적 테마 주입 구조로 정리
- [x] 효과: 교사 인원이 몇 명이 되더라도 라벨 구분성이 유지되며, 같은 교사는 항상 동일한 배지 톤을 사용
- [ ] 실기기 회귀: 다수 교사(5명+) 동시간대에서 배지 색 구분이 충분하고 텍스트 대비가 유지되는지 PASS 확인

### 학생관리 63차 구현 반영(교사 배지 유사색 충돌 완화)
- [x] 화면 내 교사 배지 색상 계산 시 hue 간격(최소 각도)을 강제해 비슷한 색상 충돌을 완화
- [x] 동일 교사는 기존처럼 안정적인 고정색을 유지하고, 같은 화면에서 다른 교사는 가능한 한 멀리 떨어진 색으로 재배치
- [x] 효과: 교사 수가 많아도 "색이 거의 같은 배지" 체감이 줄어 담당 교사 식별성이 개선
- [ ] 실기기 회귀: 4명 이상 동시 노출 화면에서 교사 배지 색상이 직관적으로 구분되는지 PASS 확인

### 학생관리 64차 구현 반영(출석 완료 일정이 결석으로 재변경되는 회귀 보정)
- [x] 전문가 분석(데이터키/운영/출석판정): DB `scheduled_time`(HH:MM:SS)와 앱 로컬 키(HH:MM) 불일치 시 자동 결석 로직이 기존 출석 상태를 미인식하고 `absent`로 재저장되는 경로를 확인
- [x] 시간 키 정규화 유틸(`normalizeScheduleTimeKey`)을 추가하고 출석 동기화/로딩/자동 결석 처리 전 구간에 적용
- [x] 자동 결석 처리 시 같은 슬롯의 기존 상태 여부를 정규화 키 기준으로 검사하도록 보강(이미 상태가 있으면 결석 덮어쓰기 금지)
- [x] 효과: 이미 출석/지각/보강 처리된 일정이 업데이트/타이머 과정에서 반복적으로 결석으로 바뀌는 현상 완화
- [ ] 실기기 회귀: `출석 처리 후 자동타이머/재진입/새로고침` 이후에도 상태가 결석으로 바뀌지 않는지 PASS 확인
- [x] 실기기 보고 템플릿 제공: `docs/checklist.md`에 학생관리 58~64차 통합 PASS/FAIL 1줄 템플릿 추가
- [x] 실기기 점검 순서 제공: `docs/checklist.md`에 학생관리 58~64차 1->7 실행 순서 추가

### 학생관리 65차 구현 반영(출석기록 메타 가시화: 처리방식/인증시간/자리확인/처리시간)
- [x] 출석기록 상세 카드에 `처리방식` 배지를 추가(`QR 스캔/번호 입력/선생님 체크/임시출석/미처리`)
- [x] `인증시간`과 `처리시간`을 분리 표기해 "언제 인증했고 언제 확정됐는지"를 한눈에 확인 가능하게 보강
- [x] `자리확인` 배지를 추가해 재석 확인 경로(확인함/미확인)를 카드 단위로 즉시 파악 가능하게 보강
- [x] 구현 방식: DB 스키마 변경 없이 기존 필드(`qr_scanned`, `qr_scan_time`, `qr_judgment`, `memo`, `check_in_time`) 조합으로 메타 해석
- [ ] 실기기 회귀: QR/번호입력/재석확인/임시출석 4개 경로가 출석기록 카드에 의도대로 표시되는지 PASS 확인

### 학생관리 66차 구현 반영(출석 메타 DB 정규화 + 하위호환 폴백)
- [x] `attendance_records` 메타 컬럼 정규화 SQL 추가(`ATTENDANCE_RECORD_META_UPDATE.sql`)
  - `attendance_source(qr/phone/teacher/emergency/unknown)`, `auth_time`, `presence_checked`, `processed_at`
- [x] `SUPABASE_COMPLETE_SETUP.sql`에 동일 컬럼 생성/보강 로직 동기화
- [x] `saveAttendanceRecord` 저장 시 정규화 메타를 함께 업서트하고, 운영 DB 미적용 환경에서는 레거시 payload로 자동 재시도 폴백 추가
- [x] 출석 조회 쿼리를 `select(*)`로 확장해 신규 메타 컬럼을 앱 전역에서 활용 가능하도록 보강
- [x] 정리 1차: JS 문법 점검(`qr-attendance.js`, `script.js`, `database.js`) 통과
- [x] 운영 적용 확인: 메타 컬럼 API 조회(`attendance_source/auth_time/presence_checked/processed_at`) 200 응답 확인
- [ ] 실기기 회귀: QR/번호입력/수동/임시/재석확인 경로에서 카드 메타(`처리방식/인증시간/자리확인/처리시간`)가 DB 값과 일치하는지 PASS 확인

### 학생관리 67차 구현 반영(관리자 인증 시 타교사 일정 수정/삭제 허용 복원)
- [x] 타교사 일정 모달 진입 시 관리자 PIN 인증 문구를 `조회 전용`에서 `조회/수정 가능` 정책으로 변경
- [x] 관리자 인증 성공 시 출석 모달을 `읽기전용 해제`로 열고 상태/메모/일정변경/일정삭제를 허용하도록 보강
- [x] 모달 컨텍스트(`adminOverride`)를 권한 검증에 연결해 일정 변경/삭제 시 `관리자 권한` 성공 토스트가 일관되게 노출되도록 정리
- [ ] 실기기 회귀: A교사가 B교사 일정 진입 시 관리자 인증 후 `상태변경/시간수정/메모저장/단건삭제`가 모두 동작하는지 PASS 확인

### 학생관리 68차 구현 반영(묶음 블록 담당 라벨 `선생님` 폴백 축소)
- [x] `getTeacherNameById`에서 `teacher.id`/`owner_user_id` 매칭을 대소문자/공백 무시 비교로 보강
- [x] UUID/난독화 키가 아닌 레거시 식별자(예: `선생님3`)는 `선생님`으로 덮지 않고 라벨로 그대로 노출하도록 보강
- [x] 효과: 묶음 블록 헤더가 단순 `담당 선생님`으로 떨어지는 케이스를 줄이고 식별 가능한 담당 라벨을 유지
- [ ] 실기기 회귀: 문제가 난 23:30~01:10 묶음 블록에서 `담당 선생님` 대신 실제 라벨(이름/식별자)이 표시되는지 PASS 확인

### 학생관리 69차 구현 반영(묶음 블록 담당 라벨 미복구 시 교사별 별칭 표시)
- [x] 전체 보기 묶음 헤더 라벨 계산에 `resolveTeacherLabelForEvent`를 추가해 이름 해석 실패를 중앙 처리
- [x] 이름 매칭 실패 시 교사 키 기준으로 `선생님A/B/C...` 별칭을 부여해 블록 간 담당자 구분이 가능하도록 보강
- [x] 효과: `담당 선생님` 일반 라벨 고정 문제를 제거하고, 최소한 교사별로 다른 라벨이 보이도록 개선
- [ ] 실기기 회귀: 예시 시나리오(`A:학생1,2` / `B:학생2,3`)에서 묶음 2개가 서로 다른 담당 라벨(`선생님A`/`선생님B` 또는 실제 이름)로 표시되는지 PASS 확인

### 학생관리 69차 구현 반영(전체 보기 묶음 규칙 고정: 교사 owner 기준)
- [x] `renderDayEvents`의 묶음 그룹 키를 학생 fallback보다 `raw teacher_id(스케줄 소유 교사)` 우선으로 고정
- [x] 효과: 예시 시나리오(`A: 1,2 / B: 2,3`)에서 학생 2가 겹쳐도 `A 묶음`과 `B 묶음`이 분리 유지
- [x] 라벨 복원 보강: `teacherNameLookup`에 canonical key(특수문자 제거)까지 저장해 owner id 포맷 차이로 인한 `선생님` 폴백 감소
- [ ] 실기기 회귀: `전체 선생님` 보기에서 동시간대 `A(1,2)`/`B(2,3)`가 별도 묶음 + 담당 라벨로 표시되는지 PASS 확인

### 학생관리 69차 구현 반영(전체 일정 로드시 타교사 이름맵 동기화)
- [x] `loadAllTeachersScheduleData`에서 owner 범위 `teachers(id, owner_user_id, name)`를 함께 조회해 라벨 맵을 갱신
- [x] `getTeacherNameById`가 `teacherNameLookup` 우선 참조 후 기존 매칭/폴백을 수행하도록 보강
- [x] 효과: 타교사 일정이 `teacherList`에 즉시 반영되지 않은 시점에도 묶음 블록의 담당 라벨 복원률 개선
- [ ] 실기기 회귀: 전체 보기에서 묶음 블록 담당 라벨이 `선생님` 일반값 대신 실제 담당명으로 표시되는지 PASS 확인

### 학생관리 70차 구현 반영(묶음 라벨 실명 우선 복원: 학생-교사 매핑 점수화)
- [x] 묶음 이벤트 멤버 기준으로 등록 선생님 매핑(`teacher_students_mapping`, `student.teacher_id`)을 점수화해 담당명 후보를 추론
- [x] `groupedEvents` 구성 단계에서 `teacherName`이 `선생님`일 경우 추론 결과를 우선 반영
- [x] 최종 배지 라벨 계산(`resolveTeacherLabelForEvent`)에서도 추론 로직을 재적용해 실명 복원 우선순위를 강화
- [x] 효과: `선생님A/B` 별칭 fallback 전에 등록된 선생님명이 최대한 우선 표시되도록 개선
- [ ] 실기기 회귀: 예시 시나리오(`A: 학생1,2` / `B: 학생2,3`)에서 묶음 라벨이 실제 등록 선생님명으로 표시되는지 PASS 확인

### 학생관리 71차 구현 반영(출석이 결석으로 되돌아가는 구조 이슈 보정)
- [x] 자동 결석 처리(`autoMarkAbsentForPastSchedules`) 전에 DB 최신 상태를 확인해 `present/late/makeup`이면 결석 덮어쓰기를 중단
- [x] 수업 관리 모달의 상태 저장(`setAttendance`)을 현재 로그인 선생님이 아닌 `att-owner-teacher-id` 기준으로 저장하도록 보정
- [x] 메모 저장(`saveOnlyMemo`)도 동일하게 owner teacher 기준 조회/저장으로 정합성 통일
- [x] 출석 조회(`getAttendanceRecordByStudentAndDate`)에 `scheduled_time` 포맷 보정(`HH:MM`/`HH:MM:SS`) 재시도를 추가
- [x] 자동 결석의 DB 사전확인에서 `teacher_id 일치 조회 -> teacher 무관 보조 조회` 순으로 확인해 과거 오저장 레코드도 역전 방지
- [x] 효과: 타교사 일정을 관리자 모드로 편집한 뒤 상태가 다시 결석으로 역전되는 경로를 차단
- [ ] 실기기 회귀: 결석->출석 변경 후 일정 수정/재진입/자동타이머 이후에도 DB/화면이 출석 유지되는지 PASS 확인

### 학생관리 72차 구현 반영(owner teacher 식별 정규화 보강)
- [x] 수업 모달 owner 해석(`resolveOwnerTeacherIdForModal`)에서 레거시 teacher key 후보를 `resolveKnownTeacherId`로 정규화해 실제 등록 teacher.id를 우선 선택
- [x] 효과: 모달 진입 시 owner teacher가 현재 로그인 교사로 잘못 폴백되어 저장 teacher_id가 틀어지는 경로를 추가 차단
- [ ] 실기기 회귀: 묶음 일정 진입 후 상태/메모 저장 시 DB `teacher_id`가 실제 담당 선생님으로 유지되는지 PASS 확인

### 학생관리 72차 구현 반영(레거시 teacher_id 정규화 + 자동결석 슬롯 매칭 보강)
- [x] `teacherScheduleData`에 남아 있는 레거시 teacher key를 학생-교사 매핑 점수 기반으로 등록된 teacher id로 병합 정규화
- [x] 정규화 적용 시 레거시 로컬키(`teacher_schedule_data__<legacy>`)를 정리하고 대상 teacher 로컬 저장을 즉시 갱신
- [x] 자동 결석 DB 확인 시 `HH:MM` 뿐 아니라 `HH:MM:00` 슬롯도 함께 조회해 시간 포맷 불일치로 인한 오판을 감소
- [x] 효과: 묶음 블록 담당 라벨의 실명 복원률을 높이고, `출석 -> 결석` 역전 재발 경로(teacher_id/시간키 불일치)를 추가 차단
- [ ] 실기기 회귀: 문제가 난 `23:30~01:10` 묶음에서 등록 선생님명이 표시되고, 결석->출석 변경 후 결석으로 재역전되지 않는지 PASS 확인

### 학생관리 73차 구현 반영(묶음 라벨 실명 우선 고정 + 레거시 키 강제 해석)
- [x] `resolveKnownTeacherId`를 canonical 비교(대소문자/특수문자 정규화)까지 확장해 혼합 포맷 teacher key 매칭률 보강
- [x] 전체 보기 렌더에서 레거시 `teacher_id`를 학생-교사 점수 기반으로 등록 teacher id로 즉시 해석(`legacyTeacherResolutionMap`)하도록 추가
- [x] 묶음 그룹키(`groupTeacherKey`)에도 위 해석값을 반영해 같은 교사의 레거시 key 혼입 시에도 일관된 블록/라벨 유지
- [x] `선생님A/B` fallback을 제거하고, 실명 복원 실패 시 라벨을 `담당 미확인`으로 고정해 등록명 누락 상태를 명확하게 노출
- [x] 효과: 운영자가 기대한 "등록된 선생님명 우선 표시"를 기본 정책으로 강화하고 별칭 혼선을 제거
- [ ] 실기기 회귀: 예시 시나리오(`A: 학생1,2` / `B: 학생2,3`)에서 묶음 라벨이 실제 등록 선생님명으로 표시되는지 PASS 확인

### 학생관리 74차 구현 반영(출석 역전 재발 차단: 날짜동기화 병합 + 오저장 결석 정리)
- [x] 날짜별 출석 동기화(`ensureAttendanceForDate`)를 owner 전체 teacher 범위 조회로 확장하고, 동일 슬롯 중복 레코드는 상태 우선순위(`present/late/makeup > absent > none`) + 최신 시각으로 병합
- [x] 관리자 모드 타교사 저장 시 남아 있던 과거 `currentTeacherId` 결석 그림자 레코드를 조건부 정리(같은 슬롯에 현재교사 실제 일정이 없고, 저장상태가 출석계열일 때만 정리)
- [x] 효과: 과거 오저장 결석이 최신 출석값을 덮는 경로를 줄여 `결석 -> 출석` 변경 후 재진입/후속 처리에서 결석 재표시되는 재발 가능성을 추가 차단
- [ ] 실기기 회귀: 문제 시나리오(묶음 일정, 관리자 모드, 결석->출석 변경)에서 재진입/자동타이머 이후에도 출석 유지되는지 PASS 확인

### 학생관리 75차 구현 반영(슬롯 단건 조회 우선순위 보강: 복수 레코드 선택 안정화)
- [x] 슬롯 단건 조회(`getAttendanceRecordByStudentAndDate`)에서 `maybeSingle` 복수 결과 발생 시 `created_at 최신 1건` 단순 선택을 중단
- [x] 복수 레코드는 `teacher_id 일치 > scheduled_time(정규화 포함) 일치 > 상태 우선순위(present/late/makeup 우선) > 최신 시각` 순으로 점수화해 최적 레코드 선택
- [x] 동일 보정 로직을 `scheduled_time` fallback 경로에도 적용해 과거 잘못 저장된 결석 레코드가 조회 우선으로 선택되는 확률을 축소
- [x] 효과: 관리자 모드/레거시 데이터가 혼재한 상태에서도 `결석 -> 출석` 변경 이후 조회 시 결석 레코드가 다시 선택되는 재발 경로를 추가 차단
- [ ] 실기기 회귀: 문제 시나리오(묶음 일정 + 관리자 모드)에서 상태 변경 직후/재진입/자동타이머 후 조회값이 출석계열로 유지되는지 PASS 확인

### 학생관리 75차 구현 반영(출석 단건 조회/메모저장 보정: 다중 레코드 우선순위 + 결석 강제저장 차단)
- [x] `getAttendanceRecordByStudentAndDate` 다중 레코드 충돌 시 `limit(1)` 단순 최신 선택을 제거하고, `teacher_id`/`scheduled_time` 일치 + 상태 우선순위(`present/late/makeup > absent > none`) + 최신 시각 기준으로 단건 선택
- [x] `scheduled_time` fallback 조회(`allowScheduledFallback`)에서도 동일 우선순위 선택기를 적용해 과거 그림자 결석 레코드가 최신 출석을 덮지 않도록 보강
- [x] 상태 저장(`persistAttendanceStatusToDbForTeacher`)에서 `teacherId` 우선순위를 `existing.teacher_id`보다 인자값(모달 owner teacher) 우선으로 고정해 오저장 재유입을 차단
- [x] 메모 저장(`saveOnlyMemo`) 시 기존 레코드를 못 찾았을 때 상태를 `absent`로 강제 업서트하던 경로를 제거하고 `local slot status -> none` 순서로 보수 저장
- [ ] 실기기 회귀: 묶음 일정(관리자 모드)에서 `결석->출석` 변경 후 `메모저장/재진입/자동타이머`까지 거쳐도 `결석`으로 재역전되지 않는지 PASS 확인

### 학생관리 76차 구현 반영(묶음 담당 라벨 미복원 보정: 멤버 후보 재해석 + 중복 접두어 제거)
- [x] 묶음 라벨 결정(`resolveTeacherLabelForEvent`)에서 기존 해석 실패 시 멤버별 teacher 후보(`member.teacherId`, `student.teacher_id`, `teacher_students_mapping`)를 다시 수집해 다수결로 등록 선생님명을 복원
- [x] 멤버 후보 해석은 `resolveKnownTeacherId`를 재사용해 레거시 owner key/혼합 포맷 id를 등록 `teacher.id`로 정규화한 뒤 이름 조회
- [x] 모달 owner 해석 우선순위(스케줄 owner -> 학생 teacher -> 배정 teacher -> 현재 teacher)를 라벨 fallback에도 반영해 모달/블록 담당명이 불일치하는 케이스를 축소
- [x] 최종 폴백 문구를 `담당 미확인`에서 `미확인`으로 조정해 배지 CSS의 `담당` 접두어와 중복(`담당 담당 미확인`)되지 않도록 정리
- [x] 효과: 같은 담당 선생님의 묶음 일정이 `미확인`으로 떨어지는 비율을 줄이고, 실패 시에도 라벨 문구가 자연스럽게 표시됨
- [ ] 실기기 회귀: 동일 담당 묶음 블록에서 배지가 실제 등록 선생님명으로 노출되고 `담당 담당` 중복이 사라졌는지 PASS 확인

### 학생관리 77차 구현 반영(시간표 렌더 중단 긴급복구: 라벨 fallback 참조 오류 수정)
- [x] `renderDayEvents` 내부 라벨 fallback 함수(`resolveTeacherNameByModalPolicy`)가 외부 변수 `ev`를 잘못 참조하던 런타임 오류를 수정
- [x] 함수 입력을 `members` 중심에서 이벤트 객체(`ev`)로 정정하고 호출부도 동일하게 변경
- [x] 효과: 시간표 렌더링이 중간에 중단되며 축선/블록이 사라지던 현상을 복구
- [ ] 실기기 회귀: 전체 선생님 보기에서 시간축/일정 블록이 정상 노출되고 스크롤/검색/묶음라벨이 함께 동작하는지 PASS 확인

### 학생관리 78차 구현 반영(출석 재역전 보강: 초기 동기화 범위/병합 규칙 수정)
- [x] 초기 출석 동기화(`loadAndCleanData`)를 `currentTeacherId` 필터에서 owner 전체 레코드 조회로 전환
- [x] 동일 슬롯 중복 병합 규칙을 `최신시각 우선` 단독에서 `상태 우선순위(present/late/makeup > absent > none) + 동률 시 최신시각`으로 보강
- [x] 효과: 관리자 모드로 타교사 출석을 `출석`으로 바꾼 뒤, 다른 결석 그림자 레코드가 재로딩 시 다시 덮는 경로를 추가 차단
- [ ] 실기기 회귀: 문제 시나리오(묶음 일정/타교사 수정)에서 `출석 변경 -> 재진입/새로고침` 이후에도 결석 재역전이 없는지 PASS 확인

### 학생관리 79차 구현 반영(로그 분석 기반 핫픽스: owner 전체 조회 필터 명시 지원)
- [x] 로그 분석 결과: `getAttendanceRecordsByOwner(null)` 호출이 내부 fallback(`teacherId || currentTeacherId`) 때문에 실제로는 현재 교사 필터로 조회되던 구조 확인
- [x] `database.js`의 `getAttendanceRecordsByOwner`를 보강해 `teacherId === undefined`일 때만 현재교사 fallback, `null/''`은 owner 전체 조회로 명시 처리
- [x] 효과: 초기 동기화에서 교사 필터가 의도치 않게 적용되는 경로를 제거해 타교사 슬롯 출석값 누락/역전 가능성을 추가 감소
- [ ] 실기기 회귀: 묶음 일정(타교사 owner)에서 결석->출석 변경 후 새로고침 시에도 상태가 출석으로 유지되는지 PASS 확인

### 학생관리 80차 구현 반영(묶음 일정 owner 폴백 수정: 현재교사 강제치환 제거)
- [x] `resolveOwnerTeacherIdForModal`에서 known teacher id 미해석 시 무조건 `currentTeacherId`로 치환하던 fallback을 제거
- [x] 후보 owner 라벨이 현재 선생님명과 일치할 때만 현재교사 id로 정규화하고, 그 외에는 원본 legacy owner key를 유지
- [x] 효과: 묶음 일정(legacy owner key 포함) 편집 시 저장 `teacher_id`가 schedule owner와 어긋나지 않아 결석 재역전 경로를 추가 차단
- [ ] 실기기 회귀: 박스형 묶음 일정에서 `결석->출석` 변경 후 재진입/새로고침 시 결석 재역전이 사라졌는지 PASS 확인

### 학생관리 81차 구현 반영(묶음 모달 슬롯 정합화: owner/시간 재매칭 보강)
- [x] `openAttendanceModal` 진입 시 요청 슬롯(startTime)이 현재 owner 스케줄에 없으면 `resolveScheduleOwnerTeacherId`로 owner를 재보정
- [x] 모달 대상 슬롯 탐색을 문자열 완전일치에서 `normalizeScheduleTimeKey` 기반 비교로 변경(`HH:MM`/`HH:MM:SS` 혼합 대응)
- [x] 효과: 묶음 블록 클릭 시 23:30 일정이 18:00 같은 다른 슬롯으로 저장되는 경로를 차단해 `수정 후 다시 결석` 체감 재발을 추가 축소
- [ ] 실기기 회귀: 23:30~01:10 묶음 블록에서 상태 변경 시 저장 로그의 `startTime`이 실제 슬롯(23:30)으로 유지되고 재진입 후 상태가 유지되는지 PASS 확인

### 학생관리 80차 구현 반영(묶음 일정 재역전 보강: 자동결석 비교/그림자 정리 확장)
- [x] 자동결석 사전 확인에서 `owner teacher` 조회와 `owner 전체` 조회를 모두 수행하고, 상태 우선순위(`present/late/makeup > absent > none`) + 최신시각 비교로 최종 상태를 선택
- [x] `cleanupLegacyAbsentShadowRecord`를 현재교사 1개 id 정리에서 `owner teacher 외 전체 teacher_id` 정리로 확장
- [x] 정리 대상은 `status=absent` + 동일 슬롯 + 실제 스케줄 미보유 teacher_id로 제한해 정상 레코드 삭제를 방지
- [x] 효과: 묶음 일정에서 legacy/타교사 결석 그림자 레코드가 다시 선택되어 `출석 -> 결석`으로 역전되는 경로를 추가 차단
- [ ] 실기기 회귀: 묶음 일정(타교사 owner)에서 `결석->출석` 변경 후 메모저장/재진입/새로고침/자동타이머 이후에도 출석 유지되는지 PASS 확인

### 학생관리 81차 구현 반영(묶음 일정 owner/start 정합성 보강)
- [x] 일정 owner 해석(`resolveScheduleOwnerTeacherId`)의 시간 비교를 문자열 완전일치에서 시간 정규화(`HH:MM`) 비교로 변경
- [x] 수업 관리 모달의 일정 미탐지 fallback 시간을 고정 `16:00` 대신 클릭된 슬롯(`startTime`) 우선으로 적용
- [x] 효과: 묶음 블록에서 owner teacher/start slot 해석 불일치로 다른 시간대에 저장되는 경로를 축소
- [ ] 실기기 회귀: 묶음 블록에서 클릭한 슬롯과 저장 슬롯이 일치하고, 상태 변경 후 재진입 시 동일 슬롯 상태가 유지되는지 PASS 확인

### 학생관리 82차 구현 반영(묶음 일정 저장 슬롯 가드: owner 일정 슬롯 강제 보정)
- [x] `setAttendance` 저장 직전에 `resolveAttendanceSlotStartTime`를 적용해 owner teacher 일정 슬롯과 일치하는 시간으로 보정
- [x] `saveOnlyMemo` 저장도 동일 보정 함수를 사용해 메모 저장이 다른 슬롯(`18:00` 등)에 기록되는 경로를 차단
- [x] 효과: 묶음 박스 일정에서 모달 시간/레거시 키 혼선이 있더라도 저장은 실제 owner 슬롯 기준으로 고정되어 `출석 -> 결석` 재역전 위험을 추가 감소
- [ ] 실기기 회귀: 23:30~01:10 묶음 블록에서 상태/메모 저장 후 콘솔 `startTime`과 DB `scheduled_time`이 23:30으로 유지되는지 PASS 확인

### 학생관리 82차 구현 반영(자정 넘김 수업 자동결석 판정 보정)
- [x] 자동결석(`autoMarkAbsentForPastSchedules`)의 `dateStr < today` 즉시 결석 후보 분기를 제거
- [x] 각 슬롯마다 `dateStr + startTime + duration`으로 실제 종료시각(`classEnd`)을 계산해 `nowKst >= classEnd`일 때만 결석 후보 처리
- [x] 효과: `23:30~01:10` 같은 야간/묶음 수업이 자정 직후 조기 결석 처리되어 `출석 -> 결석`으로 되돌아가는 경로를 차단
- [ ] 실기기 회귀: 자정 넘김 수업에서 00:00~종료시각 구간에는 상태가 유지되고, 종료시각 이후에만 자동결석이 동작하는지 PASS 확인

### 학생관리 83차 구현 반영(묶음 일정 재현 로그 보강: 저장/자동결석 추적)
- [x] 상태 저장 경로(`persistAttendanceStatusToDbForTeacher`)에 `[ATT-BOX][persist][write]` 로그를 추가해 `teacherId/scheduledTime/status`를 즉시 확인 가능하도록 보강
- [x] 자동결석 저장 경로(`autoMarkAbsentForPastSchedules`)에 `[ATT-BOX][autoAbsent][write]` 로그를 추가해 결석 write 발생 시점을 슬롯 단위로 추적 가능하도록 보강
- [x] 효과: 커서/브라우저가 불안정한 환경에서도 재현 1회 로그만으로 어떤 경로가 `absent`를 다시 쓰는지 빠르게 판단 가능
- [ ] 실기기 회귀: 재역전 재현 시 `[ATT-BOX]` 로그 2종으로 write 순서(출석 저장 vs 자동결석 저장)를 확인해 원인 경로를 확정

### 학생관리 84차 구현 반영(일정 삭제 시 동일 슬롯 출석기록 동시 삭제)
- [x] 일정 삭제 보조 함수 `deleteAttendanceRecordBySlotFromDb`를 추가해 `student/date/teacher/scheduled_time` 기준으로 출석 레코드를 함께 삭제하도록 보강
- [x] 단건 삭제(`deleteSingleSchedule`)에서 일정 삭제와 동일 슬롯 출석 삭제를 병렬 처리하도록 변경
- [x] 기간 삭제(`executeBulkDelete`, `executePeriodDelete`)에서 삭제 대상 슬롯을 수집해 로컬 출석/메모 캐시(`attendance/records/shared_records`)와 DB 출석 레코드를 함께 정리하도록 확장
- [x] 기간 삭제 경로에서 로컬 슬롯 누락 케이스를 대비해 DB `schedules(schedule_date/start_time)`를 먼저 조회한 뒤, 조회된 슬롯의 출석 레코드만 삭제하도록 정밀화(날짜 범위 전체 삭제 제거)
- [x] 기간 삭제 확인문구를 "출석기록도 함께 삭제" 정책으로 갱신해 실제 동작과 안내를 일치화
- [x] 정적 검증: `node --check script.js` + `ReadLints(script.js)` PASS
- [ ] 실기기 회귀: 일정 삭제(단건/기간/기간별) 후 출석기록 화면에서 같은 날짜·시간 슬롯 기록이 즉시 사라지고 재진입/새로고침 후에도 복원되지 않는지 PASS 확인

### 학생관리 85차 구현 반영(일정-출석 슬롯 정합성 보강)
- [x] 전문가 분석(프론트엔드/데이터정합성): 시간표 렌더에서 출석 상태 조회가 문자열 키 완전일치(`ev.originalStart`)에 의존해 `HH:MM`/`HH:MM:SS` 혼합 시 슬롯 매칭이 어긋날 수 있음을 확인
- [x] 시간표 렌더(`renderDayEvents`)의 출석 상태 조회를 `getSlotValueByNormalizedTime` 기반으로 통일해 슬롯 단위 매칭을 정규화
- [x] 출석 이력 모달(`loadStudentAttendanceHistory`)의 교사 분류를 `resolveKnownTeacherId` 정규화 비교로 보강해 일정/출석의 교사 기준 일치율을 개선
- [x] 출석 이력 모달 슬롯 키 생성에서 시간 정규화를 `normalizeScheduleTimeKey` 우선 사용으로 통일
- [x] 정적 검증: `node --check script.js` + `node --check qr-attendance.js` + `ReadLints` PASS
- [ ] 실기기 회귀: "시간표 일정 상태"와 "출석기록 카드 상태/시간"이 동일 슬롯 기준으로 일치하는지 확인(특히 다중시간대/레거시 교사키 혼합 데이터)

### 학생관리 86차 구현 반영(출석이력 슬롯 중복 레코드 우선순위 정렬)
- [x] 전문가 분석(프론트엔드/데이터정합성): 출석이력은 동일 슬롯 중복 레코드가 있을 때 첫 건을 그대로 사용해 시간표(우선순위 병합)와 표기 불일치가 발생할 수 있음을 확인
- [x] 출석이력 모달에서 슬롯별 레코드를 교사 단위로 압축하고, 상태 우선순위(`present/late/makeup > absent > none`) + 최신시각 기준으로 대표 레코드를 선택하도록 보강
- [x] primary/other 슬롯 레코드 맵 모두 동일 압축 규칙을 적용해 카드 본문과 툴팁의 상태 불일치를 완화
- [x] 정적 검증: `node --check qr-attendance.js` + `node --check script.js` + `ReadLints` PASS
- [ ] 실기기 회귀: 3/5 케이스에서 시간표 상태와 출석기록 카드/툴팁 상태가 같은 슬롯 기준으로 일치하는지 확인

### 학생관리 87차 구현 반영(출석기록 `미처리` 선택 시 슬롯 레코드 실삭제)
- [x] 전문가 합의(운영/데이터정합성): 출석기록에서 `미처리`로만 보정하면 이전 테스트 레코드가 DB에 남아 시간표-이력 불일치가 재발할 수 있으므로 슬롯 레코드 실삭제 정책이 필요함을 확정
- [x] 출석기록 모달 상태변경(`updateAttendanceStatusFromHistory`)에서 `미처리` 선택 시 해당 슬롯(`student/date/teacher/scheduled_time`) DB 레코드를 즉시 삭제하도록 보강
- [x] 삭제 후 로컬 캐시(`students/currentTeacherStudents`)도 슬롯 단위로 제거해 시간표/출석기록 UI가 같은 기준으로 즉시 동기화되도록 보강
- [x] 정적 검증: `node --check qr-attendance.js` + `node --check script.js` + `ReadLints` PASS
- [ ] 실기기 회귀: 육효원 3/5 케이스에서 `미처리` 선택 직후 시간표/출석기록 불일치가 해소되고 재진입 후에도 재발하지 않는지 확인

### 학생관리 88차 구현 반영(출석이력 `미처리` 변경 미반영 핫픽스)
- [x] 출석이력 상태변경 드롭다운에 `teacher_id`/`record_id` 힌트를 연결해 화면에서 선택한 실제 레코드를 우선 식별하도록 보강
- [x] `updateAttendanceStatusFromHistory` 삭제 경로를 `record_id -> teacher_id 힌트 -> 기본 교사 -> 슬롯 기반` 순서로 재탐색하도록 보강
- [x] `scheduled_time`이 없는(무일정/레거시) 레코드도 `is null` 조건으로 삭제되도록 삭제 쿼리 분기 추가
- [x] 로컬 메모리 정리 로직에서 `default` 슬롯(시간 미지정) 제거를 명시적으로 처리하도록 보강
- [x] 정적 검증: `node --check qr-attendance.js` + `ReadLints(qr-attendance.js)` PASS
- [x] 실기기 회귀: 육효원 3/5 케이스에서 `미처리` 변경 직후 카드 상태가 즉시 반영되고, 새로고침/재진입 후에도 재발하지 않는지 확인

### 학생관리 89차 구현 반영(출석 조회 400 방지: 시간 필터 검증)
- [x] `scheduled_time` 필터 적용 전 시간 리터럴 검증(`HH:MM`/`HH:MM:SS`) 유틸 추가
- [x] 비시간 값(`default` 등)은 조회 필터에서 제외하고 경고 로그만 남기도록 보강
- [x] 슬롯 삭제 쿼리에서도 유효 시간값만 `in` 필터로 사용하고, 비시간/미지정은 `scheduled_time is null` 분기로 처리
- [x] 정적 검증: `node --check qr-attendance.js` + `ReadLints(qr-attendance.js)` PASS
- [x] 실기기 회귀: 3/5 케이스 재현 시 콘솔 `attendance_records 400`이 재발하지 않는지 확인

### 학생관리 90차 구현 반영(출석기록 카드 담당 선생님 배지 상시 노출)
- [x] 출석기록 카드 헤더의 담당 선생님 배지를 fallback 케이스 전용에서 전 슬롯 상시 노출로 전환
- [x] 현재 슬롯의 레코드/일정/담당 교사 정보를 우선순위로 해석해 배지 텍스트(`담당 {선생님명}`)를 안정적으로 계산
- [x] 내부 식별자(UUID) 문자열이 배지에 노출되는 경우 사용자용 라벨(`담당 미확인` 또는 담당명)로 대체
- [x] 정적 검증: `node --check qr-attendance.js` + `ReadLints(qr-attendance.js)` PASS
- [ ] 실기기 회귀: 3/5 케이스에서 `전재윤` 담당 배지가 `선생님3` 배지와 동일한 형태로 노출되는지 확인

### 학생관리 91차 구현 반영(출석기록 카드 X삭제 + 시간표 일치 경고)
- [x] 출석기록 카드 헤더에 건별 `X` 삭제 버튼 추가(레코드가 있는 카드만 노출)
- [x] 삭제 동작은 카드 레코드 힌트(`record_id`/`teacher_id`)를 사용해 해당 출석기록만 직접 삭제 경로로 연결
- [x] 시간표와 불일치(해당 슬롯 schedule 없음) 기록은 즉시 삭제, 시간표 일치 기록은 경고 확인창 후 삭제되도록 분기 추가
- [x] 삭제 후 시간표/이력 즉시 동기화를 위해 기존 상태변경 삭제 경로(`updateAttendanceStatusFromHistory`)를 공통 재사용
- [x] 정적 검증: `node --check qr-attendance.js` + `ReadLints(qr-attendance.js)` PASS
- [x] 실기기 회귀: 불일치 카드 `X` 클릭 시 즉시 삭제되고, 일치 카드 `X` 클릭 시 경고창이 노출되는지 확인

### 학생관리 92차 구현 반영(출석기록 조회 400 재발 방지 가드)
- [x] `getAttendanceRecordByStudentAndDate` 조회 전에 `studentId`(숫자)/`attendance_date`(YYYY-MM-DD) 유효성 검사 추가
- [x] 비정상 파라미터는 Supabase 요청을 보내지 않고 경고 로그 후 조회 생략(return null) 처리
- [x] `processAttendanceFromQR` 일정없음 분기의 임시출석 중복조회에도 동일 파라미터 가드 적용
- [x] `getAttendanceRecordsByDate`에도 날짜 형식 가드 추가로 잘못된 조회 파라미터 차단
- [x] 정적 검증: `node --check qr-attendance.js` + `ReadLints(qr-attendance.js)` PASS
- [x] 실기기 회귀: 출석기록 화면/QR 진입에서 `attendance_records GET 400` 반복이 재발하지 않는지 확인

### 학생관리 93차 구현 반영(출석상태 라벨 충돌 방지: `청구됨` 오표기 수정)
- [x] 출석기록 전용 상태 헬퍼를 `attendanceStatusToLabel`/`getAttendanceStatusStyle`로 분리해 전역 함수명 충돌 차단
- [x] 수납 화면 상태 헬퍼를 `paymentStatusToLabel`로 명확히 분리해 출석/수납 라벨의 책임 경계를 고정
- [x] 정적 검증: `node --check qr-attendance.js` + `node --check js/payment.js` + `ReadLints(qr-attendance.js, js/payment.js)` PASS
- [ ] 실기기 회귀: 출석기록 툴팁/카드 상태가 `청구됨` 대신 `출석/지각/결석/보강/미처리`로 노출되는지 확인

### 학생관리 94차 구현 반영(출석이력 툴팁 타교사 노출 조건 정합화)
- [x] 출석이력 툴팁의 타교사 노출 조건을 `otherRecords or otherSchedules`에서 `otherSchedules(실제 슬롯 일정)` 기준으로 축소
- [x] 슬롯에 일정이 없는 타교사 잔존 레코드는 툴팁에서 제외해 "시간표에는 없는데 툴팁에는 보임" 혼선을 차단
- [x] 정적 검증: `node --check qr-attendance.js` + `ReadLints(qr-attendance.js)` PASS
- [ ] 실기기 회귀: 3/5 케이스에서 타교사 슬롯 일정이 없으면 툴팁에 해당 교사가 노출되지 않는지 확인

### 학생관리 95차 구현 반영(타교사 `미처리` 유령 툴팁 보정)
- [x] 출석이력 교사키 정규화에 `primaryTeacher.owner_user_id -> primaryTeacher.id` 컨텍스트 매핑을 추가해 동일 교사의 이중 키(owner/id) 분리 경로 차단
- [x] 타교사 툴팁 후보는 `정규화 teacher_id` 기준으로 재집계하고, 실제 스케줄 행이 없는 후보는 렌더에서 제외
- [x] 정적 검증: `node --check qr-attendance.js` + `ReadLints(qr-attendance.js)` PASS
- [ ] 실기기 회귀: 3/5 케이스에서 Supabase에 타교사 일정이 없으면 툴팁의 타교사 `미처리` 행이 사라지는지 확인

### 학생관리 96차 구현 반영(주담당 기준키 우선순위 보정)
- [x] 출석이력의 주담당 기준키를 `assignedTeacherId 우선`에서 `currentTeacherId 우선`으로 변경
- [x] 주담당 정규화 id를 `current -> assigned -> fallback` 순으로 계산해 교사키 혼합(owner/id) 환경의 오분류 경로 축소
- [x] 상단 `담당 선생님` 칩도 정규화된 주담당 id를 우선 사용하도록 동기화
- [x] 정적 검증: `node --check qr-attendance.js` + `ReadLints(qr-attendance.js)` PASS
- [ ] 실기기 회귀: 3/5 케이스에서 타교사 일정 없음 상태의 툴팁 `미처리` 유령 행 미노출 확인

### 학생관리 97차 구현 반영(툴팁 generic 교사키 필터)
- [x] 타교사 툴팁 후보에서 `선생님/미확인/담당 미확인` 등 generic 이름으로 해석되는 teacher key를 제외
- [x] 실제 스케줄이 있어도 교사 식별이 불가한 레거시 키는 툴팁 교차교사 비교에서 제거해 운영 혼선 축소
- [x] 정적 검증: `node --check qr-attendance.js` + `ReadLints(qr-attendance.js)` PASS
- [ ] 실기기 회귀: 3/5 케이스에서 generic 타교사(`선생님`) `미처리` 툴팁 행 미노출 확인

### 학생관리 98차 구현 반영(담당선생님 불일치 + 일정수정 중복생성 동시 보정)
- [x] 수업관리 모달 오픈 시 슬롯 owner를 `resolveExactSlotOwnerTeacherId`로 재확정해 담당 선생님 라벨/권한 기준을 슬롯 실체 기준으로 고정
- [x] 모달에 `att-original-owner-teacher-id`를 추가해 일정 변경 시 "원본 소유 teacher"를 유지 추적
- [x] 일정 변경(`updateClassTime`)에서 구 슬롯 삭제 대상을 단일 teacher에서 `원본 owner + 슬롯 owner 후보군`으로 확장해 "삭제 실패 후 신규 추가" 중복생성 경로 차단
- [x] 정적 검증: `node --check script.js` + `ReadLints(script.js, index.html)` PASS
- [x] 실기기 회귀: (1) 모달 상단 담당선생님이 슬롯 담당과 일치하는지 (2) 일정 변경 후 기존 일정이 갱신되고 중복 생성되지 않는지 확인

### 학생관리 99차 구현 반영(툴팁 노출 조건 명확화)
- [x] 타교사 툴팁 유효행(`validOtherTeachers`)을 선계산해 "실제 타교사 행이 1개 이상일 때만" 툴팁이 열리도록 조건 보강
- [x] 타교사 후보가 전부 필터링되는 케이스(스케줄 없음/generic 라벨)는 `data-has-tooltip` 자체를 비활성화해 단독(주담당만) 툴팁 노출 방지
- [x] 정적 검증: `node --check qr-attendance.js` + `ReadLints(qr-attendance.js)` PASS
- [ ] 실기기 회귀: 3/5 케이스에서 타교사 유효행이 없으면 툴팁이 아예 뜨지 않는지 확인

### 학생관리 100차 구현 반영(툴팁 범위 정책: 같은 날짜 타교사 다른 시간 표시)
- [x] 타교사 툴팁 집계를 슬롯 단위(`otherScheduleBySlot`)에서 날짜 단위(`otherSchedulesByDate`)로 확장
- [x] 같은 날짜의 타교사 `teacher+time` 조합을 중복 제거 후 표시해 "메인=담당, 툴팁=타교사 다른 시간" 정책 반영
- [x] 각 타교사 시간행의 상태는 해당 타교사/해당시간 슬롯 레코드로 매칭하고, 없으면 `미처리`로 표시
- [x] 정적 검증: `node --check qr-attendance.js` + `ReadLints(qr-attendance.js)` PASS
- [ ] 실기기 회귀: 같은 날짜 다른 시간 타교사 수업이 있으면 툴팁에 해당 시간행이 표시되는지 확인

### 학생관리 101차 구현 반영(내 학생 필터 정합성: 타교사 학생 혼입 차단)
- [x] 전문가 합의(프론트엔드/데이터정합성/운영): `내 학생` 필터 기준을 로컬 매핑 단독에서 `students.teacher_id` 우선으로 고정
- [x] `getAssignedStudentIdsForTeacher`를 보강해 학생 `teacher_id`와 현재 교사 키가 일치하는 학생만 기본 포함하고, 로컬 매핑은 `teacher_id`가 비어있는 학생에 한해 보조 사용
- [x] `getAssignedTeacherId`도 학생 `teacher_id`를 우선 참조하도록 변경해 배정 select의 기준 교사가 stale 매핑 순서에 흔들리지 않게 보강
- [x] 정적 검증: `node --check script.js` + `ReadLints(script.js)` PASS
- [ ] 실기기 회귀: `선생님3(담당 0명)` 계정의 `내 학생` 탭이 0명으로 표시되고, `전재윤(담당 6명)` 계정은 기존 인원 유지되는지 확인

### 학생관리 102차 구현 반영(Supabase SQL 정돈: 보안/정합성 통합 점검 스크립트)
- [x] core SQL 정리용 통합 스크립트 추가(`SUPABASE_CORE_SECURITY_MAINTENANCE.sql`)
- [x] 점검 구간 추가: RLS 활성 상태, 과허용 owner 정책(`owner_user_id IS NOT NULL`), 공개 정책(`USING true`, `WITH CHECK true`) 탐지
- [x] 보정 구간 추가(DRY-RUN 기본): owner 정책 강화, 공개 read 제한(옵션), `homework_submissions.teacher_id` 타입 정합성(TEXT) 보정
- [x] 운영 안전장치: `apply_changes=false` 기본 + 옵션 플래그(`keep_homework_public_flow`)로 기능 회귀 위험 최소화
- [ ] 운영 실행: Supabase SQL Editor에서 `[A] 점검 -> [B] apply_changes=true 반영 -> [C] 재점검` 순서로 결과 확인

### 학생관리 103차 구현 반영(Supabase SQL 타입보정 실패 재발 방지)
- [x] 운영 에러(`cannot alter type of a column used in a policy`) 원인 반영: `homework_submissions.teacher_id` 타입 변경 전에 정책 의존성 정리 단계 추가
- [x] `SUPABASE_CORE_SECURITY_MAINTENANCE.sql` 보강: `homework_submissions` 정책을 임시로 내리고 타입/FK 보정 후 기본 정책(`homework_owner_policy`, `homework_public_insert`, `homework_public_read`)을 조건부 복구
- [x] 공개 정책 제한 옵션(`restrict_public_read_policies`)과 `keep_homework_public_flow` 조합 시 복구 정책이 충돌하지 않도록 분기 정리
- [ ] 운영 실행: 동일 스크립트를 `apply_changes=true`로 재실행해 타입 보정 완료 및 정책 복구 결과 확인

### 학생관리 104차 구현 반영(Supabase 타입 정합성 2차: `teachers.id` 기준 자동 정렬)
- [x] 운영 에러(`foreign key ... incompatible types: text and uuid`) 원인 반영: `homework_submissions.teacher_id`를 고정 `text`가 아닌 `teachers.id` 실제 타입 기준으로 정렬하도록 수정
- [x] `SUPABASE_CORE_SECURITY_MAINTENANCE.sql` 보강: `teachers.id` 타입 조회 후 `homework_submissions.teacher_id`를 `uuid/text`로 동적 변환
- [x] UUID 변환 시 비정상 문자열을 `NULL`로 정리한 뒤 캐스팅해 변환 실패를 방지하도록 보강
- [ ] 운영 실행: `apply_changes=true` 재실행 후 `homework_submissions.teacher_id` 타입이 `teachers.id`와 일치하는지 확인

### 학생관리 105차 구현 반영(Supabase 테이블 헬스체크 스크립트 추가)
- [x] 핵심 테이블 점검 SQL 추가(`SUPABASE_TABLE_HEALTH_CHECK.sql`)
- [x] 점검 항목 구성: 테이블 존재/행수, 핵심 컬럼 타입, FK 제약, 고아 데이터, RLS/정책 위험 태그, 인덱스
- [x] 운영 안전성: READ ONLY 전용으로 구성(데이터 변경 없음)
- [ ] 운영 실행: Supabase SQL Editor에서 전체 실행 후 결과 캡처 기준으로 PASS/보정 필요 항목 분류

### 학생관리 우선순위(전체 시스템 보정판)
1. [x] QR 긴급출석(임시출석) 도입 - 일정 미등록 시에도 출석 흐름 유지
2. [x] 임시출석 확정 워크플로우 - 확정 대기함/수동 확정/사유 기록
3. [x] 일정 누락 사전 예방 - 당일 누락 알림 + QR 진입 전 경고 + 자동 생성 제안
4. [x] 학생 상세 통합 화면 액션화 - 조회 중심에서 조치 중심으로 확장
5. [ ] 테스트 점수 원격 동기화 운영 검증(실데이터 저장/조회/삭제 + 폴백) - SQL/RLS 반영 PASS, 앱 실데이터 2개 시나리오 검증 진행 예정(학생관리 28차)
6. [x] 학생 스키마 확장 SQL 운영 반영 검증(`STUDENT_SCHEMA_UPDATE.sql`) - SQL 실행 + 검증 쿼리 PASS
7. [x] 테스트 점수 SQL 운영 반영 검증(`STUDENT_TEST_SCORE_SETUP.sql`) - SQL 실행 + 정책 검증 PASS
8. [ ] 권한/다교사/학부모 커뮤니케이션 정책 정리(운영 고도화)

## 현재 우선 작업 (재개 후)
1. [x] `results.py` full regrade 오류 분기 보강(Drive 다운로드 실패 / ZIP 손상 / 이미지 0건)
2. [x] 오류 시나리오 모킹 검증(Drive 실패 502 / ZIP 형식 오류 400 / 이미지 0건 400)
3. [ ] 대용량 ZIP/Drive 실데이터 시나리오 검증(토스트/진행률 포함 E2E 정합성)
   - 진행상태: 일부 수행(`result_id=34` full regrade 재실행 시 채점 시간 초과 실패 관측)
   - 남은범위: 생성된 픽스처(`qa-artifacts/regrade-fixtures`)로 실데이터 제출을 만들고 케이스별 재현
   - 최신상태: 긴급 우회(`USE_GRADING_AGENT=false`) 적용 후 재검증에서 `use_grading_agent=false` 반영 확인, `regrade_trigger=200` 확인. poll 후반부 `results/progress`가 회복되며 `result_id=34`는 `review_needed`(score 100/500, uncertain 3)로 수렴했고 timeout 실패 메시지는 재발하지 않음
4. [x] 채점 타임아웃 동적화(이미지 수 기반, 상한 포함) 적용
5. [x] 운영 반영 판별용 런타임 헬스 엔드포인트 추가(`/health/runtime`)
6. [x] 채점 단계 추적/타이브레이크 안전장치 추가(시간초과 원인 분석용)

## 긴급 복구 작업 (2026-03-08)
- [x] 머지 충돌 마커 정리: `script.js`, `qr-attendance.js`, `index.html`, `docs/*`, `.cursor/rules/workflow-doc-driven.mdc`
- [x] 정적 검증: `rg`로 충돌 마커 미존재 확인 + `node --check script.js` + `node --check qr-attendance.js` 통과
- [x] 다음 단계: 실기기 회귀 1순위 3건(묶음 본인 인증/메인 진입 성능/초기 배지 집계) 즉시 검증 및 결과 문서 반영
- [x] 다음 단계: 공휴일 키 경고 재발 여부(강력 새로고침)와 메인 회귀 잔여 2건(백그라운드 동기화/툴팁 정확성) 확인
- [x] 다음 단계: 학생관리 87차 실기기 회귀(미처리=슬롯 레코드 실삭제 정책) 마감
- [ ] 다음 단계(우선순위 1): 학생관리 86차 실기기 회귀(출석이력 중복 레코드 우선순위 정합성) 마감
- [ ] 다음 단계(우선순위 2): 학생관리 85차 실기기 회귀(일정-출석 슬롯 정합성) 마감
- [ ] 다음 단계(우선순위 3): 학생관리 66차 실기기 회귀(메타 표시 정합성) 마감

## 브랜드 리디자인 스프린트 (하이로드 수학)
- [x] 브랜드 토큰(네이비/골드, 깔끔한 톤) 정의 및 공통 스타일 반영
- [x] 메인/출석 화면(`index.html`, `style.css`, `mobile.css`) 1차 리디자인
- [x] 결과 화면(`grading/index.html`) 브랜드 정합성 반영
- [x] 서브 화면(`parent-portal`, `homework`, `css/sub-shared.css`) 톤 통일

## 오늘 할 일 (Top 3)
1. [x] 하이로드 수학 브랜드 토큰 정의 및 주요 문구 통일
2. [x] 메인/결과/서브 화면 브랜드 스타일 일괄 반영
3. [x] 리디자인 진행 상태를 `context.md`와 `checklist.md`에 반영

## 후속 작업 (2차 정합성)
1. [x] 인라인 하드코딩 브랜드 색상(`index.html`) 2차 정리
2. [x] 사용자 알림/메일 템플릿 브랜드 문구 통일(`parent-portal`, `send-reset-code`)
3. [x] 브라우저 실동작 수동 QA(데스크톱/모바일) 수행 및 캡처 기록

## 작업 상태
| 항목 | 상태 | 담당 | 비고 |
|---|---|---|---|
| 브랜드 토큰 정립 (`style.css`, `css/sub-shared.css`) | DONE | me/ai | 네이비/골드 기반 토큰 적용 |
| 메인/출석 UI 브랜딩 (`index.html`) | DONE | me/ai | 제품명 `하이로드 수학` 반영 |
| 결과 화면 브랜딩 (`grading/index.html`) | DONE | me/ai | 채점 화면 테마/타이틀 정합성 반영 |
| 서브 화면 브랜딩 (`parent-portal`, `homework`) | DONE | me/ai | 공통 톤 통일 |
| 인라인 색상/문구 2차 정합성 (`index.html`, 메일 템플릿) | DONE | me/ai | 구 보라/구브랜드 문구 추가 정리 |
| 시나리오 기반 수동 QA | DONE | me/ai | `/`, `/grading/`, `/parent-portal/`, `/homework/` 데스크톱/모바일 점검 및 캡처 완료 |
| 최종 패치 재검증 (`/grading`, `/parent-portal`, `/homework`) | DONE | me/ai | CORS 재발 없음, 390px 레이아웃 깨짐 없음, `mathlive` 요청 실패/헤더 골드 포인트 약함은 잔존 |
| 안정화 후속 패치 (`mathlive` 지연 로딩, 헤더 골드 디바이더) | DONE | me/ai | 첫 진입 안정성 강화 및 서브 화면 브랜드 포인트 보강 |
| `grading` 수식 편집 영향도 검증 (`mathlive` `net::ERR_ABORTED(HEAD)`) | DONE | me/ai | 강제 모달 오픈/입력/저장/닫기 PASS, CDN 차단 시 raw fallback 자동 전환 확인 |
| 결과 API 검증 강화 (`results.py` 입력 검증) | DONE | me/ai | `update_item` 허용 필드/타입 검증 + 음수/잘못된 타입 차단 |
| `/grading/` 첫 진입 스모크 재확인 (렌더/탭/콘솔) | DONE | me/ai | 로컬 실접속 200 + `quick-recheck` 대조로 PASS, 콘솔 에러 없음 |
| 결과 화면 폴링 안정성 보강 (`grading/index.html`) | DONE | me/ai | 폴링 fetch timeout/실패 카운트/지연 경고 토스트 추가 |
| 결과 API 입력 검증 2차 (`regrade`, `feedback`) | DONE | me/ai | 양의 정수/허용 타입/문자열 정제 검증 추가, 무효 입력 400 처리 |
| 장시간 채점/대용량 E2E (경고 노이즈/자동복구) | DONE | me/ai | 격리 러너(`tmp-e2e-runner`)로 실측 완료, 진행률 경고 1회/복구 후 노이즈 없음 확인 |
| 상세 폴링 경고 트리거 보강 (`grading/index.html`) | DONE | me/ai | `results` 단독 실패 누적도 경고 대상으로 반영 |
| 상세 완료 토스트 중복 방지 (`grading/index.html`) | DONE | me/ai | 동일 `result_id` 완료 이벤트 반복에서도 성공 토스트 1회만 허용 |
| 전체 재채점 시작→진행률/완료 반영 E2E 정합성 | DONE | me/ai | `regradeWithKey(full_regrade)` 분기, 진행률 폴링 시작, 완료 시 결과 재로딩까지 실측 PASS |
| full regrade 오류 분기 보강 (`results.py`) | DONE | me/ai | Drive 실패(502), ZIP 형식/손상/빈 파일(400), 이미지 0건(400) 구분 처리 |
| full regrade 오류 분기 모킹 검증 | DONE | me/ai | 함수 하네스로 3개 케이스 상태코드/메시지 확인(502/400/400) |
| full regrade 실데이터 검증 (`result_id=34`) | DONE | me/ai | API 시작 응답 200 확인, 이후 진행률 `failed`/결과 `review_needed` + timeout 에러메시지 관측 |
| 오류 유형별 실데이터 케이스 재현(다운로드/ZIP손상/이미지0건) | TODO | me/ai | 테스트용 제출 데이터셋 준비 필요(현재 운영 데이터 1건으로는 케이스 분리가 어려움) |
| 채점 타임아웃 동적화 (`grading.py`, `config.py`) | DONE | me/ai | 고정 300초 대신 `base + per_image * n`(max 제한) 적용, timeout 메시지에 기준 시간/이미지 수 반영 |
| 운영 API 응답성 재측정(5/10/20초) | DONE | me/ai | `results`/`grading-progress`/`items` 모두 200 응답 확인(일시적 ReadTimeout 해소) |
| 동적 timeout 적용 효과 실측(`result_id=34`) | DONE | me/ai | `result_id=34`/`35` 장시간 관측(20~30회 poll)에서 API 오류 0건/상태 수렴 확인. StageTiming 직접 캡처는 로그 노이즈로 보류하되 운영 안정성 기준으로 PASS 마감 |
| 운영 반영 확인용 런타임 헬스 엔드포인트 (`main.py`) | DONE | me/ai | `/health/runtime`에 timeout/feature 플래그 노출(배포 후 동적 timeout 값 반영 여부 즉시 확인 가능) |
| 운영 반영 여부 확인(`/health/runtime`) | DONE | me/ai | 운영 서버 200 확인 + `grading_timeout_base/per_image/max` 값 노출 확인(배포 반영 완료) |
| 채점 단계 타이밍 로그/timeout 원인 표시 보강 | DONE | me/ai | `preprocess→ocr→cross_validate→agent_verify→grading→saving` 단계 전환/소요를 로그에 남기고 timeout 메시지에 마지막 단계 포함 |
| OCR 타이브레이크 안전장치 보강 | DONE | me/ai | 이미지당 타이브레이크 항목 상한 + 문제당 재시도 상한 + 거부응답 즉시 fallback 추가 |
| `agent_verify` 근본 수정(하드 timeout/문제수 상한/잔여시간 fallback) | DONE | me/ai | 단계별 hard timeout, 질문 상한(`AGENT_VERIFY_MAX_QUESTIONS`), 잔여시간 부족 시 agent 단계 생략 반영 |
| 오류 재현 픽스처 생성(`qa-artifacts/regrade-fixtures`) | DONE | me/ai | `no_images.zip`, `empty.zip`, `not_a_zip.bin` 생성 + 사용 가이드 문서화 완료 |
| 런타임/재채점 통합 점검 스크립트 추가 | DONE | me/ai | `qa-artifacts/run_runtime_regrade_check.py` 추가, 운영 리포트(`runtime-regrade-check-report.json`) 생성 |
| 배포/반영 검증 절차 문서화 | DONE | me/ai | `qa-artifacts/deploy-and-verify-runtime.md`에 배포→`/health/runtime`→통합점검 재실행 절차 고정 |
| 배포 후 검증 실행 스크립트 추가/실행 | DONE | me/ai | `qa-artifacts/verify_runtime_after_deploy.ps1`/통합 스크립트로 재검증 수행(`health_runtime=200`, `result34`는 기존 `review_needed`) |
| 5분 배포 체크리스트 제공 | DONE | me/ai | `qa-artifacts/deployment-checklist-quick.md` 작성(배포 전/배포 확인/운영 판정/실패 조치) |
| 배포 사전 준비상태 점검 | DONE | me/ai | `predeploy-readiness.md` 작성: 로컬 변경 미커밋/미푸시 상태 확인(배포 전 커밋·푸시 필요) |

상태 기준: `TODO` / `IN_PROGRESS` / `DONE` / `BLOCKED`

## 작업 인계 메모 (다음 단계)
- 현재 상태: 재채점 안정화 트랙은 운영 안정성 기준으로 PASS 마감(`result_id=35` 장시간 관측 30/30, 에러 0)
- 다음 작업(우선순위): 학생 관리 + 수납 관리 대규모 업데이트 기획(세무/교육/UX 관점) 착수
- 다음 단계 권장 순서:
  1) 수납관리 기능 요구사항을 "세무 신고 편의" 기준으로 분해(필수 데이터/증빙/정산)
  2) 학생관리-수납관리 데이터 모델/화면 흐름을 통합 설계
  3) 1주차 범위(핵심 CRUD + 영수증/납부내역 + 리포트 기초)로 MVP 구현 시작

## 차기 트랙 킥오프 (학생/수납 대규모 업데이트)
- 목표: 수납 누락 방지 + 세금 신고 준비 시간 단축 + 원장/학부모 커뮤니케이션 일관성 확보
- 1차 범위: 학생관리 핵심 정보 정비 + 수납 ledger(청구/수납/환불/미수금) + 월별 리포트/CSV
- 1차 제외: 카드사·PG 실시간 정산 연동, 전자세금계산서 자동발행, 고급 회계 분개 자동화

### 전문가 합의 기반 1주차 실행안
1) 데이터 모델
   - 학생: 재원상태, 시작일/종료일, 보호자 연락처, 반/코스 매핑
   - 수납: 청구월, 납부일, 수납수단(현금/계좌/카드), 공급가/세액, 증빙유형, 환불 여부/사유
2) UX/화면
   - 학생 상세에 수납 탭 고정(월별 ledger 타임라인)
   - 상태 뱃지(완납/부분/미납/환불) + 월마감 체크
3) 세무 편의 산출물
   - 월별 수납 합계, 미수금 리스트, 수단별 합계, 환불 내역 CSV

### 결제 채널 선정 기준(결제선생 vs Bizzle)
1) 통일 원칙: SMS 간편결제 + 현장결제를 한 채널에서 처리 가능하면 단일 채널 우선
2) 필수 검증 항목: 환불/부분취소, 정산주기, 수수료, 거래ID 추적, CSV 품질
3) 의사결정 방식: 1~2주 파일럿 후 월마감 대사 시간/오류율 기준으로 최종 선택

### API 없이 시작하는 3채널 수납 운영안(v1)
1) 채널 역할 고정
   - 비대면 링크 결제: 결제선생
   - 대면 결제(단말/동백전): 비즐
   - 계좌이체: 사업자 통장
2) 공통 원장(ledger) 필드 강제
   - 학생ID, 청구월, 결제채널, 결제수단, 거래참조ID, 결제일시, 공급가, 세액, 총액, 환불여부
3) 일마감 루틴(5분)
   - 채널별 합계(결제선생/비즐/통장) = 앱 원장 합계 대사
   - 미확인 입금(통장 입금자명 미매칭) 큐 분리
4) 월마감 산출물
   - 월별 수납 합계, 미수금 리스트, 수단별 합계, 환불 내역 CSV

### 수납 화면 설계 우선순위(실행 순서)
1) 학생/원장 기본 입력폼 + 상태배지 먼저 고정
   - 목표: 사람이 빠르게 입력/확정하고, 미수금/부분수납을 즉시 식별
2) 사진 업로드 + AI 추출 보조(검토 후 확정) 적용
   - 목표: 수기 입력량 감소, 단 확정권한은 사용자에게 유지
3) 일마감/월마감 화면 및 리포트 연결
   - 목표: 채널별 합계 대사와 세무 제출용 CSV를 일관되게 생성

### 1순위 구현 반영(학생/원장 입력폼 + 상태배지)
- [x] 수납 모달에 `원장 입력` 버튼과 입력 모달 추가(학생/청구월/청구금액/수납금액/결제채널/결제수단/거래참조ID/미확인입금/메모)
- [x] 학생 카드 상태 배지를 `청구됨/부분수납/완납/미확인입금` 중심으로 재정의
- [x] 카드 상세에 원장 메타정보(채널/수단/수납일/거래참조) 노출 및 학생별 원장 수정 진입 추가

### 2순위 구현 반영(사진 업로드 + AI 추출 + 검토 팝업)
- [x] 수납 모달에 `증빙+AI` 버튼 및 업로드 모달 추가(학생 힌트/청구월 힌트/증빙유형/이미지 파일)
- [x] 백엔드 `POST /api/payments/extract` 추가(Gemini 기반 이미지 추출 → 원장 초안 JSON 반환)
- [x] 프론트 검토 팝업 추가(자동 추출값 표시 + 검토 포인트 노출 + 확정 저장/미확인입금 저장)
- [x] AI 저장 시 `evidenceType=ai_extract`, `evidenceName` 저장으로 추적성 보강

### 3순위 구현 반영(일마감/월마감 + CSV)
- [x] 수납 모달에 일마감 요약 패널 추가(오늘 수납액/건수, 월 미수금, 미확인입금)
- [x] 채널별 월 수납 합계 카드 추가(결제선생/비즐/통장/기타)
- [x] 월마감 CSV 4종 버튼 구현(월 원장, 미수금, 수단별 합계, 환불내역)
- [x] 원장/AI 검토 화면에 환불금액·환불사유 필드 추가 및 순수납(수납-환불) 기준 집계 반영
- [x] 미확인입금 저장 시 학생 선택 필수 조건 해제(학생 미지정 저장 허용, 별도 큐 저장)
- [x] 잘림 이슈 즉시 패치(카드 펼침 자동 스크롤 + list overflow 보정 + 모바일 모달 높이 보정)
- [x] 노트북 해상도 대응 보강(요약 패널 접기/펼치기 + 자동 컴팩트 모드)
- [x] 데스크톱/노트북 수납 작업성 보강(수납 모달 폭 확대: 560px → 900~960px)
- [x] 미확인입금 큐 화면 추가(월 기준 목록 + 학생 연결 + 큐 삭제)
- [x] 카드 상세영역 내부 스크롤 추가(잘림/겹침 대응)
- [x] 수납 카드 아코디언화(한 번에 1개만 펼침) + 상세 높이 확장
- [x] 카드 상세를 오버레이 표시로 전환(하단 카드 밀림 방지)
- [x] AI 추출 다건 모드(1장 이미지 -> 여러 건 `drafts[]`) + 다건 검토 일괄 저장 모달 추가(선택형)
- [x] 수납/비용 분리 구조 1차 반영(별도 탭 + 비용 등록 모달 + 월별 비용 목록/요약 + 비용 CSV)
- [x] 비용 원장 Supabase 동기화 연동(가능 시 원격 동기화, 미구성 시 로컬 폴백)
- [x] `expense_ledgers` DB 스키마 + owner 기준 RLS SQL 스크립트 추가(`EXPENSE_LEDGER_SETUP.sql`)
- [x] 비용 탭에서 수납(학생 목록/필터/요약)이 함께 노출되는 숨김 처리 버그 수정(`.hidden` 공통 유틸 추가)
- [x] 일마감 대사 차이 계산 UI 추가(외부 채널 실합계 입력 + 채널별/합계 차이 표시, 월별 저장)
- [x] 비용 등록 모달에 인건비/강사비 조건부 상세 필드 추가(지급대상/지급월/원천세/공제)
- [x] 수납/비용 세무 공통 필드 추가(공급가액/세액/증빙유형/증빙번호) + CSV 반영
- [x] 비용 원장 세무 상세(공급가액/세액/증빙)를 Supabase 컬럼으로 구조화 저장(메모 파싱 의존 제거 + 구버전 테이블 fallback)
- [x] 대사 차이값 + 인건비 상세 메타를 월 원장/수단별 CSV에 함께 내보내는 옵션 추가
- [x] 수납/비용 모달에 입력/자동계산/AI추출 구분 UI(테두리/배지) + 자동 계산 + 필수 누락 하이라이트 적용
- [x] 어려운 세무 용어를 쉬운 라벨로 병기하고, 마우스오버 툴팁 설명 추가(귀속월/공급가액/세액/부가세구분/증빙)
- [x] `?` 클릭형 도움말 팝업 추가(모바일 포함, 키보드/ESC 닫기 지원)
- [x] 자주 쓰는 표현으로 용어 정리(결제채널→결제경로, 거래참조ID→거래확인번호, 미확인입금 큐→입금 확인대기함)
- [x] `대사 차이 합계` 용어를 `장부와 실제 합계 차이`로 변경 + 즉시 이해용 설명 문구/도움말 추가
- [x] 전문가 합의 문서화: 주민번호 미저장 원칙(인건비 신고친화), 세무 필수항목 우선 검증, 면세 기본값 일괄 고정 지양
- [x] 세무 신고 일정 안내 박스 + 신고 완료 확인창(해당 기간/대상일 때 자동) 추가
- [x] 수납 모달 스크롤/가시성 버그 수정(요약 자동 접힘 과도 기준 완화 + 사용자 접힘 상태 저장 + 학생목록 영역 스크롤 복구)
- [x] 월별 세무 체크리스트 카드 추가(비용 증빙 정리/인건비 지급/전월 원천세 확인)
- [x] 비용 모달 인건비 세무 분기 강화(비율제/월급제 소득유형 분리 + 비율제 3.3% 자동계산 + 실지급액 자동계산 + 유형별 필수검증)
- [x] 수납 탭 데스크톱 2열 레이아웃 적용(좌: 요약/세무/도구, 우: 학생목록 고정 스크롤)
- [x] 수납 탭 좌측(요약/세무) 패널 독립 스크롤 보강(요약 펼침 시 하단 잘림 방지)
- [x] 월별 노무/회계 체크리스트 추가(요약 패널 내 체크/저장)
- [x] 체크리스트 조건부 활성화 적용(현재 월/인건비 지급/원천세 점검 기간 기준으로 필요한 항목만 활성)
- [x] AI 증빙 추출 이미지 Google Drive 자동 저장(수납증빙/학원명또는owner/YYYY/MM/DD/항목) + 원장 메모에 Drive 경로 자동 기록
- [ ] 다음 단계(구현): 회계사 제출 고정 포맷(월 신고팩) 정의 후 저장단계 세무 필수 검증(증빙번호/거래처/과세구분) 강화 + 용어집을 모든 세무 필드로 확장
- [ ] 다음 단계(구현): 인건비 상세를 원격 동기화 가능한 구조 컬럼(또는 JSON)로 확장해 기기 간 동일한 소득유형/공제/실지급 집계가 유지되도록 개선
- [ ] 다음 단계(학생관리): 학생별 상태/연락/수납연동 정보 스키마 정리 및 UI 입력폼 우선 구현

### 재개용 즉시 실행 체크리스트 (복붙 순서)
1) 로컬 상태 확인: `git status --short`
2) 미배포라면 커밋/푸시 후 Railway redeploy
3) 런타임 확인: `curl https://academymanager-production.up.railway.app/health/runtime`
4) 재검증 실행:
   - `python "qa-artifacts/run_runtime_regrade_check.py" --base-url "https://academymanager-production.up.railway.app" --teacher-id "508b3497-2923-4c16-b220-5099092dab76" --result-id 34 --trigger-regrade --timeout 30 --poll-count 8 --poll-interval 8 --out-file "qa-artifacts/runtime-regrade-check-report.json"`
5) 판정:
   - PASS: timeout 미재발 + `result_id=34` 상태 수렴
   - BLOCKED: poll 타임아웃/응답 불안정으로 최종 상태 미확정

## 완료 기준 (Definition of Done)
- [ ] 기능이 요구사항대로 동작한다.
- [ ] 예외/에러 처리 경로가 확인되었다.
- [ ] 관련 체크리스트 항목을 완료했다.
- [ ] 다음 작업자가 바로 이어서 할 수 있게 문서가 갱신되었다.

## 변경 이력
- 2026-03-13 - 학생관리 105차(Supabase 테이블 체크): 운영 DB 구조/정합성/보안 상태를 한 번에 검증할 수 있는 `SUPABASE_TABLE_HEALTH_CHECK.sql`을 추가(READ ONLY)
- 2026-03-13 - 학생관리 104차(Supabase 타입 정합성 2차): `homework_submissions.teacher_id` 타입 보정을 `teachers.id` 기준 동적 정렬로 변경하고, UUID 캐스팅 전 비정상 값을 NULL로 정리해 FK 타입 불일치 재발을 차단
- 2026-03-13 - 학생관리 103차(Supabase 타입보정 실패 대응): `homework_submissions.teacher_id` 타입 변경 시 정책 의존성으로 실패하던 경로를 스크립트에 반영해 정책 임시삭제→타입/FK 보정→정책 복구 순서로 자동화
- 2026-03-13 - 학생관리 102차(Supabase SQL 정돈): core 보안/정합성 점검과 선택 반영을 한 파일에서 수행하는 `SUPABASE_CORE_SECURITY_MAINTENANCE.sql`을 추가(DRY-RUN 기본, owner 정책 강화/공개정책 제한/teacher_id 타입 정합성 옵션 포함)
- 2026-03-13 - 학생관리 101차(내 학생 필터 정합성): `내 학생` 목록이 stale 로컬 매핑으로 타교사 학생을 섞어 보여주던 경로를 `students.teacher_id` 우선 필터로 전환해 권한/표시 기준을 일치화
- 2026-03-13 - 학생관리 100차(툴팁 범위 정책 반영): 타교사 툴팁을 같은 슬롯 기준에서 같은 날짜 기준으로 확장해, 담당교사 카드는 메인으로 유지하고 타교사 다른 시간 수업은 툴팁에서 조회되도록 조정
- 2026-03-13 - 학생관리 99차(툴팁 노출 조건 명확화): 유효 타교사 행이 없을 때는 툴팁 자체를 비활성화해 "왜 툴팁이 뜨는지" 혼선을 제거
- 2026-03-13 - 학생관리 98차 실기기 회귀 PASS: 사용자 실기기 확인에서 담당선생님 표시 불일치 해소 및 일정 변경 시 중복생성 없이 기존 슬롯 갱신 동작 확인
- 2026-03-13 - Security Advisor 경고 해소 확인: `public.backup_attendance_teacher_fix_20260309`의 `rls_enabled=true` 반영 및 경고 소거 확인
- 2026-03-13 - Security Advisor 일괄 대응 스크립트 추가: `public.backup_*` 테이블을 한 번에 점검/보정할 수 있도록 `SUPABASE_BACKUP_RLS_BATCH_MAINTENANCE.sql`(DRY-RUN, 유지+잠금, 삭제 옵션)을 추가
- 2026-03-13 - Security Advisor 대응 SQL 추가: `public.backup_*` 테이블의 RLS 미활성 경고를 즉시 처리할 수 있도록 삭제/유지(잠금) 선택형 스크립트 `SUPABASE_BACKUP_TABLE_RLS_FIX.sql`을 추가
- 2026-03-13 - 학생관리 98차(담당불일치/중복생성 동시 보정): 모달 슬롯 owner 확정 로직을 추가하고 일정 변경 시 원본 owner 후보군 삭제를 병행해 "담당 선생님 오표기"와 "수정 대신 신규 추가" 경로를 동시에 차단
- 2026-03-13 - 학생관리 97차(툴팁 generic 교사키 필터): 타교사 툴팁에서 `선생님/미확인` 계열 generic 라벨은 비교 대상에서 제외해 유령 `미처리` 행 재노출 경로를 추가 차단
- 2026-03-13 - 학생관리 96차(주담당 기준키 우선순위 보정): 출석이력 주담당 분류 기준을 `currentTeacherId` 우선으로 고정하고 정규화 순서를 보강해 owner/id 혼합 저장 환경에서 타교사 오분류를 축소
- 2026-03-13 - 학생관리 95차(타교사 미처리 유령 툴팁 보정): owner 기반 레거시 교사키가 담당 교사와 별개 타교사로 분류되던 경로를 컨텍스트 정규화로 통합하고, 스케줄 실체 없는 타교사 툴팁 행을 렌더에서 제외
- 2026-03-13 - 학생관리 94차(툴팁 타교사 조건 정합화): 출석이력 툴팁의 타교사 노출을 실제 슬롯 일정 기반으로 제한해, 타교사 일정이 없는 슬롯에서 `선생님3`가 보이던 혼선 경로를 차단
- 2026-03-13 - 학생관리 93차(출석 라벨 충돌 수정): 수납 스크립트의 `청구됨` 라벨 함수와 출석 스크립트 상태 함수명이 전역에서 충돌하던 경로를 분리해 출석 툴팁 오표기를 차단
- 2026-03-13 - 학생관리 91/92차 실기기 회귀 PASS: 카드 `X` 삭제 즉시/경고 분기 정상 동작 및 `attendance_records GET 400` 재발 없음 확인
- 2026-03-13 - 학생관리 92차(조회 파라미터 가드): `attendance_records` 조회 전 학생ID/날짜 유효성 검사를 추가해 비정상 파라미터로 인한 GET 400 재발 경로를 차단
- 2026-03-13 - 학생관리 91차(출석기록 카드 X삭제): 카드별 삭제 버튼을 추가하고, 시간표 불일치 기록은 즉시 삭제·시간표 일치 기록은 경고 확인 후 삭제되도록 분기 적용
- 2026-03-13 - 학생관리 90차(출석기록 담당 배지 상시 노출): fallback 케이스만 보이던 담당 선생님 배지를 전 슬롯에 표시하도록 보강하고, 내부 식별자(UUID) 노출을 사용자용 라벨로 대체
- 2026-03-13 - 학생관리 88/89차 실기기 회귀 PASS: 사용자 실기기 확인에서 `미처리` 변경 미반영과 `attendance_records 400` 재발이 해소됨을 확인
- 2026-03-13 - 출석이력 `미처리` 변경 미반영 핫픽스(88차): 상태변경 시 화면 레코드 힌트(`teacher_id`/`record_id`)를 우선 사용해 삭제 대상을 정밀화하고, `scheduled_time is null` 레코드 삭제/로컬 `default` 슬롯 정리를 함께 보강
- 2026-03-09 - 66차 실기기 회귀 실행 보조: `docs/checklist.md`에 결과 입력 템플릿(요약 1줄 + 경로별 상세 + 실패 시 필수 기록)을 추가
- 2026-03-09 - 일정/출석 정합성 보정(85차): 시간표 상태 조회를 슬롯 정규화 기반으로 통일하고, 출석이력의 교사/시간 키 매칭을 정규화해 서로 다른 포맷 키(`HH:MM`/`HH:MM:SS`)로 인한 불일치 경로를 축소
- 2026-03-09 - 일정/출석 정합성 보정(86차): 출석이력 슬롯의 중복 레코드를 교사별 우선순위(상태+최신시각)로 압축해 시간표와 이력 카드/툴팁 상태 불일치를 완화
- 2026-03-09 - 일정/출석 정합성 보정(87차): 출석이력에서 `미처리` 선택 시 상태값만 바꾸지 않고 슬롯 레코드를 DB/로컬에서 함께 삭제하도록 변경해 이전 테스트 레코드 잔존으로 인한 재불일치 경로를 차단
- 2026-03-09 - 66차 운영 적용 확인: 로컬 환경키 기반 API 조회로 `attendance_records` 메타 컬럼 4종(`attendance_source/auth_time/presence_checked/processed_at`) 200 응답 확인
- 2026-03-09 - 실기기 추가 검증 PASS: 공휴일 키 경고 강력 새로고침 재발 없음, 메인 회귀 잔여 2건(백그라운드 동기화/툴팁 정확성) 정상 확인
- 2026-03-09 - 날짜 고정 재발 방지: `qa-artifacts/sync_doc_dates.py` 자동 동기화 스크립트를 추가하고 작업 시작 시 실행 규칙으로 고정
- 2026-03-09 - 문서 기준일 운영 보정: `plan/context/checklist`의 기준일을 작업 당일(2026-03-09)로 일괄 갱신
- 2026-03-08 - 실기기 회귀 1순위 3건 PASS: 묶음 본인 인증 분기, 선생님 선택 후 메인 진입 속도, `3/3·3/5` 초기 배지 집계 안정성 확인
- 2026-03-08 - 긴급 대응: 숙제 제출 페이지 모바일 접속 불가 이슈를 URL 경로 오기(`academy-manager`)로 확정하고, 정상 경로(`academy_manager/homework/`)를 운영 안내 기준으로 고정
- 2026-03-08 - 긴급 대응: 학부모 포털 모바일 접속 불가 이슈 원인을 배포 장애가 아닌 GitHub Pages 경로 오기(`academy-manager`)로 확정하고, 안내 링크를 실제 경로(`academy_manager`)로 수정
- 2026-03-08 - 긴급 안정화: 런타임/문서 파일의 머지 충돌 마커를 HEAD 기준으로 정리하고 프론트 문법검사(`script.js`, `qr-attendance.js`)를 재통과시켜 실행 가능 상태를 복구
- 2026-03-08 - 캘린더 묶음 누락/지연 6차 보정: 레거시 `teacher_id`를 학생 컨텍스트(학생 teacher_id/배정 매핑)로 재해석해 현재 교사 로드 누락을 보강하고, owner 다중매칭 이름 수렴을 단일매칭으로 제한
- 2026-03-08 - 캘린더 인원수 지연 반영 5차 보정: 전체범위(all)에서 첫 렌더 전에 전체 일정 로드를 완료 대기하도록 바꾸고 hydration 2패스 로직을 제거해 계정별 초기 집계 편차를 축소
- 2026-03-08 - 캘린더 인원수 지연 반영 4차 보정: 전체범위 hydration 패스 카운트를 도입해 2회 동기화 완료 전 집계 확정을 지연하고 1차 패스 후 자동 재동기화를 추가
- 2026-03-08 - 묶음 일정 인증 재발 8차(근본모델 보정): owner/라벨 계산에서 뷰어 의존 후보를 제거하고 라벨->teacher id(유일매칭) + 슬롯 후보 교차검증으로 owner 확정 경로를 고정
- 2026-03-08 - 묶음 일정 인증 재발 7차 보정: 라벨 힌트 기반 본인 강제 보정을 제거하고 슬롯 owner 후보 정규화 우선 정책으로 권한 판정을 일원화
- 2026-03-08 - 묶음 일정 인증 재발 6차 보정: 모달 owner 라벨 힌트를 화면 배지 라벨(`resolveTeacherLabelForEvent`)로 통일하고 관리자 인증 분기 디버그 로그를 추가
- 2026-03-08 - 묶음 일정 인증 재발 5차 보정: 모달 호출에 owner 라벨 힌트를 함께 전달하고, owner 판정 실패 시 현재 선생님명 일치 기반 최종 본인 보정을 추가
- 2026-03-08 - 묶음 일정 인증 재발 4차 보정: 묶음/단일 클릭에서 모달에 전달하는 owner를 그룹 키가 아닌 실제 `teacher_id`(`member.ownerTeacherId`) 우선으로 전달하도록 수정
- 2026-03-08 - 전체범위 초기 오집계(1명->3명) 및 묶음 본인 인증 재발 대응: `allScopeScheduleHydrated/loading` 상태 기반 배지 보정(`집계중`)과 `resolveKnownTeacherId`의 owner 다중매칭 강제수렴 제거
- 2026-03-08 - 메인 진입 성능 2차 미세튜닝: 캘린더 렌더 시 날짜 셀별 요약 반복 계산을 화면 범위 선계산 맵으로 전환해 월/주 뷰 연산량 절감
- 2026-03-08 - 선생님 선택 -> 메인 진입 지연 대응: `setCurrentTeacher`를 초기 렌더 우선 + 백그라운드 동기화 구조로 보정하고, `loadAndCleanData` 학생 조회 매핑을 `Map`으로 최적화
- 2026-03-08 - 묶음 일정 본인 클릭 관리자 인증 재발(23:30~01:10) 대응 3차 보정: 슬롯 owner 후보 전체 조회 + owner 비교 정규화 + owner 미해석 케이스 본인 일정 최종 보정 반영, 실기기 회귀 확인 대기
- 2026-03-01 - 초기 템플릿 생성
- 2026-03-01 - 문서 기반 워크플로우 적용 및 현재 작업 상태 반영
- 2026-03-01 - 기존 코드 변경분(`grading/index.html`, `grading-server/routers/results.py`) 리뷰 결과 반영
- 2026-03-01 - 하이로드 수학 브랜드 리디자인(메인/결과/서브 화면) 1차 반영
- 2026-03-01 - 후속 2차 정합성 반영(인라인 색상 및 메일/토스트 브랜드 문구 정리)
- 2026-03-01 - 리디자인 QA 수행(데스크톱/모바일) 및 이슈 정리
- 2026-03-01 - 패치 후 빠른 재검증 수행(`/grading`, `/homework`): CORS/버튼 대비/390px 레이아웃 확인
- 2026-03-01 - 최종 패치 재검증 수행(`/grading`, `/parent-portal`, `/homework`): CORS 재발 여부, `mathlive` 에러, 헤더 톤 골드 포인트, 390px 레이아웃 점검
- 2026-03-01 - 안정화 후속 패치 반영(`grading` mathlive 지연 로딩, `parent-portal`/`homework` 헤더 골드 디바이더)
- 2026-03-01 - `grading` 수식 편집 영향도 실검증: `mathlive` `HEAD` abort 경고와 편집 기능 성공/실패 fallback 경로 확인
- 2026-03-01 - 결과 API 검증 강화: `results.py`의 `update_item` 허용 필드/타입/범위 검증 추가
- 2026-03-01 - `/grading/` 첫 진입 스모크 재확인: 로컬 접속 200 및 `quick-recheck` 결과 대조로 렌더/탭/콘솔 PASS 판정
- 2026-03-01 - `grading/index.html` 폴링 안정성 보강: timeout 기반 fetch 공통화, 연속 실패 경고, 상세/진행률 폴링 회복성 향상
- 2026-03-01 - 결과 API 검증 2차: `regrade`/`feedback` 입력 정제(양의 정수, 허용 오류유형, 텍스트 길이 제한) 적용
- 2026-03-01 - 장시간 채점/대용량 E2E 실측 완료: 격리 Playwright 러너로 경고/복구 계측, 상세 경고 트리거 조건 추가 관찰 기록
- 2026-03-01 - 상세 폴링 경고 트리거 보강: 상세 요청 2개 중 하나라도 실패한 주기를 누적해 `results` 단독 실패에서도 경고 노출되도록 조정
- 2026-03-01 - 상세 완료 토스트 중복 방지: 동일 결과의 완료 상태가 연속 감지되어도 성공 토스트 중복 노출을 차단하는 가드 추가
- 2026-03-01 - 전체 재채점 E2E 정합성 검증: full regrade 시작 토스트/진행률 폴링 시작/완료 후 결과 재로딩 경로까지 격리 러너로 확인
- 2026-03-01 - 사용자 휴식 요청에 따라 인계 메모 보강: 다음 1순위/재개 절차를 문서에 고정
- 2026-03-01 - `results.py` full regrade 오류 분기 보강: Drive 다운로드 실패(502), ZIP 형식/손상/빈 파일(400), 이미지 0건(400) 구분 처리
- 2026-03-01 - full regrade 오류 분기 모킹 검증: 함수 하네스로 3개 실패 케이스(502/400/400) 응답코드/메시지 확인
- 2026-03-01 - full regrade 실데이터 검증 1건 수행: `result_id=34` 재실행 후 진행률 `failed` 및 timeout 에러메시지 확인
- 2026-03-01 - 문서 3종 정합성 업데이트: 실데이터 검증 범위를 "부분 완료 + 유형별 재현 대기"로 명확화
- 2026-03-01 - 채점 타임아웃 동적화 적용: 이미지 수 기반 timeout 계산(`base/per_image/max`) 및 timeout 안내 메시지 개선
- 2026-03-01 - 동적 timeout 적용 후 실데이터 재측정 시도: 운영 API ReadTimeout으로 상태 확인 BLOCKED, 서버 응답 안정화 후 재시도 필요
- 2026-03-01 - 운영 API 응답성 재측정: 5/10/20초 probe에서 핵심 endpoint 3종 모두 200 확인(ReadTimeout 일시 해소)
- 2026-03-01 - 운영 반영 판별용 `/health/runtime` 엔드포인트 추가: timeout/feature 런타임 설정 조회 경로 확보
- 2026-03-01 - 운영 서버 `/health/runtime` 확인 결과 404: 최신 코드 미배포 정황 문서화
- 2026-03-01 - 오류 유형 재현용 픽스처 생성: `qa-artifacts/regrade-fixtures`에 ZIP/비ZIP 샘플 및 README 추가
- 2026-03-01 - 런타임/재채점 통합 점검 스크립트 추가 및 실행: `/health/runtime` 404, `result_id=34` 상태/에러메시지 리포트 파일 생성
- 2026-03-01 - 배포/반영 검증 절차 문서화: 운영 반영 판정 기준을 `deploy-and-verify-runtime.md`로 표준화
- 2026-03-01 - 배포 검증 실행 스크립트 추가/실행: `verify_runtime_after_deploy.ps1`로 원클릭 점검(여전히 `/health/runtime` 404)
- 2026-03-01 - 원클릭 검증 재실행: `/health/runtime` 404 지속, `result_id=34` 상태 `review_needed` 유지 확인
- 2026-03-01 - 빠른 배포 점검 체크리스트 작성: `deployment-checklist-quick.md`로 즉시 실행 가능한 확인 순서 제공
- 2026-03-01 - 배포 사전점검 수행: 로컬 변경사항이 아직 원격에 반영되지 않았음을 문서화(`predeploy-readiness.md`)
- 2026-03-01 - 운영 반영 재확인: `/health/runtime` 200 및 timeout 런타임 값 노출 확인
- 2026-03-01 - 통합 점검 재실행(`trigger_regrade` 포함): 시작 200 확인, 후속 폴링 구간 ReadTimeout 반복으로 최종 판정 보류
- 2026-03-01 - 타임아웃 상향(30초) 재검증: 시작 200 + 중간 `grading(40%)` 관측, 그러나 동일 실행 내 endpoint 다수가 ReadTimeout으로 불안정
- 2026-03-01 - Railway 로그 분석 반영: `result #34` 내부 timeout(400s) 직접 관측, 단계별 타이밍 로그 + timeout 마지막 단계 노출 보강
- 2026-03-01 - OCR 타이브레이크 보호 로직 추가: 이미지당 항목 상한/재시도 상한/거부응답 즉시 fallback 및 런타임 설정 노출
- 2026-03-01 - 배포 후 검증 완료: `/health/runtime`에서 `ocr_tiebreak` 설정 노출 확인, `regrade_trigger=200(grading)` 확인, 다만 `results` poll 실패 지속
- 2026-03-01 - 긴급 우회 검증 완료: `USE_GRADING_AGENT=false` 반영 후 `result_id=34`가 timeout 없이 `review_needed`로 수렴(점수/문항 데이터 생성 확인)
- 2026-03-01 - `agent_verify` 근본 수정 반영: 단계 hard timeout + 문제 수 상한 + 전체 timeout 잔여시간 기반 조기 fallback 적용 및 런타임 설정 노출 준비
- 2026-03-01 - 재개용 인계 보강: 커밋/배포/런타임 확인/재검증 명령 순서를 문서에 고정
- 2026-03-01 - `USE_GRADING_AGENT=true` 운영 재검증 BLOCKED: `regrade_trigger=500(PGRST204, grading_items.error_type 컬럼 미존재)` 확인
- 2026-03-01 - DB 스키마 복구 후 재검증: `regrade_trigger=200`, `result_id=34` `status=confirmed` 확인으로 차단요인(500) 해소
- 2026-03-01 - 장시간 관측 재검증(20 polls): `trigger=200`, `results/progress` 오류 0건, `status=confirmed` 유지(응답 안정성 PASS)
- 2026-03-01 - 신규 제출(`result_id=35`) 장시간 관측 재검증(30 polls): `results/progress` 30/30 성공, 에러 0건, 최종 `review_needed` 수렴
- 2026-03-01 - 안정화 트랙 마감 판정: StageTiming 직접 캡처는 미완이나 운영 안정성 지표 충족으로 PASS 처리, 다음 트랙(학생/수납 대규모 업데이트)로 전환
- 2026-03-01 - 수납 AI 다건 추출(선택형) 반영: `extract_mode(single/multi)` 지원, `drafts[]` 응답/다건 검토 일괄 저장 UI 추가
- 2026-03-01 - 문서 재동기화: 현재 최우선 트랙을 학생/수납 대규모 업데이트로 명시, 재채점 트랙은 완료/모니터링 상태로 정리
- 2026-03-01 - 수납/비용 탭 분리 1차 구현: 수납 탭과 비용 탭을 분리하고 비용 원장(등록/목록/요약/CSV) 기본 흐름을 추가
- 2026-03-01 - 비용 원장 원격 동기화 1차 구현: `expense_ledgers` 테이블 연동 시 동기화, 미구성 환경에서는 기존 로컬 저장을 유지
- 2026-03-01 - 비용 원장 DB/RLS 실행 스크립트 추가: `EXPENSE_LEDGER_SETUP.sql`에 테이블/인덱스/RLS/`NOTIFY pgrst` 절차를 고정
- 2026-03-01 - 운영 규칙 추가: 롤플레잉 요청 시 전문가 의견을 답변/문서에 함께 기록하고, 불필요 요청은 전문가 섹션 생략 후 바로 응답
- 2026-03-01 - 운영 규칙 보강: 사용자 상황 미지정 시에도 요청 맥락에 맞는 전문가를 자동 선택해 의견을 포함
- 2026-03-01 - 비용 탭 노출 버그 수정: 공통 `.hidden` 유틸 누락으로 탭 숨김이 동작하지 않던 문제를 보완
- 2026-03-01 - 일마감 대사 차이 UI 반영: 외부 채널 실합계 입력(결제선생/비즐/통장/기타)과 내부 원장 대비 차이(채널별/합계)를 월 기준으로 계산/저장
- 2026-03-01 - 인건비/강사비 관리 보강: 비용 모달에서 분류 선택 시 조건부 상세 필드를 노출하고, 상세값을 메모에 구조화해 저장
- 2026-03-01 - 세무 신고 간편화 1차: 수납/비용에 공급가액·세액·증빙유형·증빙번호 입력을 추가하고 월 원장/비용 CSV 컬럼까지 확장
- 2026-03-01 - 학생등록 UX 1차 보강: 필수검증(이름/등록일/연락처), 연락처 형식검증, 동일 학생 의심 중복 차단 적용
- 2026-03-01 - 학생관리 2차 보강: 일정삭제 영향 미리보기 + 테스트 점수 입력/월별 점수 추이 UI 반영
- 2026-03-03 - 문서 기준일 갱신 + 다음 진행 작업(학생관리 3차) 우선순위 재정렬
- 2026-03-04 - 학생관리 3차 반영: 테스트 점수 Supabase 동기화(원격 우선 + 로컬 폴백) 및 SQL/RLS 스크립트 추가
- 2026-03-04 - 학생관리 4차 반영: 학생 스키마 확장(재원상태/시작일/종료일/보호자) + 등록폼/저장검증 + DB 컬럼 폴백 처리
- 2026-03-04 - 학생관리 5차 반영: 학생 상세 통합 뷰(출석/숙제/수납/테스트 요약 카드) + 이력 모달 흐름 보강
- 2026-03-04 - 학생관리 6차 반영: 일정 삭제 UX 보강(삭제 영향 경고 + 월 범위 안내 + 확인창 상세화)
- 2026-03-04 - 학생관리 7차 반영: 출석 주체 구분 강화(학생 QR/선생님 체크) + 수동 체크 시각 기록 보강
- 2026-03-04 - 학생관리 8차 반영: QR 긴급출석 1차(일정 미등록 시 임시출석 + 중복스캔 already_processed 보호)
- 2026-03-04 - 학생관리 9차 반영: 임시출석 확정 대기함/건별 확정/사유 기록 워크플로우 1차 추가
- 2026-03-04 - 학생관리 10차 반영: 임시출석 확정 워크플로우 2차(확정자 기록 + 기간 필터 + 다중 선택 일괄 확정) 추가
- 2026-03-04 - 학생관리 11차 반영: 일정 누락 사전 예방 1차(당일 경고 + QR 진입 전 확인 + 일정등록 빠른제안) 추가
- 2026-03-04 - 학생관리 12차 반영: 학생 상세 통합 화면 액션화 1차(요약 카드 액션 버튼 + 도메인별 즉시 조치 동선) 추가
- 2026-03-04 - 학생관리 13차 반영: 테스트 점수 동기화 운영 검증 1차(상태 배지 + 자동 동기화 점검 버튼) 추가
- 2026-03-04 - 학생관리 44차 반영: 전화번호 인증 UI에서 `인증`/`키패드 확인` 버튼 제거(4자리 자동인증 단일화)
- 2026-03-04 - 학생관리 45차 반영: 전화번호 인증 대상을 학생+보호자에서 학생 연락처 전용으로 제한
- 2026-03-04 - 학생관리 46차 반영: 상단 제어 버튼 제거 + 제목 더블탭 전체화면 토글 방식으로 학생 화면 오조작 방지
- 2026-03-04 - 학생관리 47차 반영: 모서리 3초 롱프레스 시 카메라 버튼 임시 노출 + 카메라 전환 PIN 인증 적용
- 2026-03-01 - 학생관리 48차 반영: 다중 수업 이력 슬롯 분리(날짜+시간) + 문자열 duration 겹침 오탐 보정
- 2026-03-01 - 학생관리 49차 반영: 다교사 시간표 범위 토글 + 전체 일정 기준 후속 알림 큐 라우팅 보강
- 2026-03-01 - 학생관리 50차 반영: 메인 헤더 시간표 토글 상시노출 + 일정 수정 권한(본인만/관리자 PIN 예외) 강화
- 2026-03-01 - 학생관리 51차 반영: 시간표 기본 스코프를 전체 공유(`all`)로 전환하고 로그인/선생님 전환 시 전체 일정 우선 표시로 고정
- 2026-03-01 - 학생관리 52차 반영: 시간표 선생님 라벨 fallback에서 UUID 노출 제거(`선생님(UUID)` -> `선생님`)
- 2026-03-01 - 학생관리 53차 반영: 시간표 선생님명 조회 시 `teacher.id` + `owner_user_id` 이중 매핑으로 담당명 노출 보강
- 2026-03-01 - 학생관리 54차 반영: 단건 일정 삭제 시 날짜 전체 출석/메모 삭제를 시간 슬롯 단위 삭제로 보정
- 2026-03-01 - 학생관리 55차 반영: 묶음 일정 블록의 담당 선생님 라벨을 학생 매핑 fallback까지 포함해 복원 보강
- 2026-03-01 - 학생관리 56차 반영: 타 교사 일정 클릭 시 관리자 비밀번호 인증 후 조회만 허용하고 편집/삭제는 전면 차단
- 2026-03-01 - 학생관리 57차 반영: 기간/일괄 삭제 경로에 소유자 컨텍스트 가드와 `내 일정만` 안내 문구를 추가
- 2026-03-01 - 학생관리 58차 반영: 상태 적용 범위 팝업 제거 후 현재 담당 선생님 일정에만 즉시 반영하도록 정책 고정
- 2026-03-01 - 학생관리 59차 반영: 묶음 일정 owner 정규화로 불필요한 관리자 인증 팝업 과노출을 보정
- 2026-03-01 - 학생관리 60차 반영: 묶음 규칙을 교사 기준으로 재정의해 동시간대 타교사 일정은 분리, 동일교사 일정만 묶음 유지
- 2026-03-01 - 학생관리 61차 반영: 시간표 교사 배지 가시성(대비/강조 텍스트) 강화
- 2026-03-01 - 학생관리 62차 반영: 교사 수가 늘어나도 자동 구분되도록 배지 색상 동적 생성 로직 도입
- 2026-03-01 - 학생관리 63차 반영: 화면 내 교사 배지 색상 유사도 충돌을 최소 각도 간격 규칙으로 완화
- 2026-03-01 - 학생관리 64차 반영: `HH:MM:SS`/`HH:MM` 시간키 불일치로 출석이 결석으로 재변경되던 회귀를 정규화 로직으로 차단
- 2026-03-01 - 문서 운영 규칙 보강: `plan/context/checklist` 3개 문서의 `문서 기준일` 불일치 시 작업 완료 금지 규칙을 강제
- 2026-03-06 - 문서 기준일 갱신: `plan/context/checklist`를 로컬 시스템 오늘 날짜(`2026-03-06`)로 동기화
- 2026-03-06 - 문서 날짜 규칙 강화: 문서 기준일은 고정값 금지, 작업일 기준으로 매일 재갱신(예: 3/6 -> 3/7)하도록 명시
- 2026-03-06 - 회귀 검증 실행 보조: 학생관리 58~64차 실기기 PASS/FAIL 복붙 템플릿을 `docs/checklist.md`에 추가
- 2026-03-06 - 회귀 검증 실행 보조 2차: 학생관리 58~64차 실기기 점검 순서(1->7)를 `docs/checklist.md`에 추가
- 2026-03-06 - 학생관리 65차 반영: 출석기록 카드에 처리방식/인증시간/자리확인/처리시간 메타를 동시 표시해 운영 추적성을 강화
- 2026-03-06 - 학생관리 66차 반영: 출석기록 메타 컬럼 정규화 SQL + 저장 폴백(미마이그레이션 자동 재시도) + 이력 표시 신규컬럼 우선 해석 적용
- 2026-03-06 - 파일 정리 1차: 66차 후속으로 JS 3개 파일 문법 점검을 완료하고(모두 PASS) 운영 SQL 적용/실기기 회귀를 다음 단계로 고정
- 2026-03-06 - 학생관리 67차 반영: 타교사 일정도 관리자 PIN 인증 성공 시 조회+수정/삭제가 가능하도록 권한 정책을 운영 요청 기준으로 복원
- 2026-03-06 - 학생관리 68차 반영: 묶음 블록 담당 라벨이 `선생님`으로만 보이는 케이스를 줄이기 위해 teacher id 해석/레거시 식별자 폴백을 보강
- 2026-03-06 - 학생관리 69차 반영: 담당 이름 해석 실패 시에도 묶음별로 `선생님A/B` 별칭을 부여해 교사별 구분이 유지되도록 보강
- 2026-03-06 - 학생관리 69차 반영: 전체 보기 묶음 키를 스케줄 owner teacher 기준으로 고정해 교사별 묶음 분리를 안정화하고 라벨 lookup을 canonical key까지 확장
- 2026-03-06 - 학생관리 69차 반영: 전체 일정 로드 시 타교사 이름맵을 함께 동기화해 묶음 블록 담당 라벨 복원을 보강
- 2026-03-06 - 학생관리 70차 반영: 묶음 멤버 기반 학생-교사 매핑 점수화로 담당 라벨을 등록 선생님명 우선 복원하도록 보강
- 2026-03-06 - 학생관리 71차 반영: 상태 저장 teacher_id 정합성과 자동결석 DB 교차검증을 보강해 `출석 -> 결석` 역전 회귀를 차단
- 2026-03-06 - 학생관리 71차 보강: `scheduled_time` 포맷 차이와 과거 teacher_id 오저장 케이스를 자동결석 사전조회에서 흡수하도록 추가 보정
- 2026-03-06 - 학생관리 72차 반영: 모달 owner teacher 식별을 레거시 key 정규화 기반으로 보강해 타교사 일정 저장 정합성을 추가 강화
- 2026-03-06 - 학생관리 72차 반영: 레거시 teacher key를 등록 teacher id로 정규화하고 자동결석 슬롯 조회(`HH:MM`/`HH:MM:00`)를 보강
- 2026-03-06 - 학생관리 73차 반영: 묶음 라벨에서 `선생님A/B` fallback을 제거하고 레거시 teacher key를 등록 teacher id로 즉시 해석하도록 보강
- 2026-03-06 - 학생관리 74차 반영: 날짜 동기화를 owner 전체 teacher 병합으로 보강하고 과거 오저장 결석(그림자 레코드) 정리 로직을 추가해 `출석 -> 결석` 역전 재발을 추가 차단
- 2026-03-06 - 학생관리 75차 반영: 슬롯 단건 조회의 복수 레코드 선택을 우선순위 점수화(teacher/time/status/최신시각)로 전환해 결석 레코드 역선택 재발을 추가 차단
- 2026-03-06 - 학생관리 76차 반영: 묶음 담당 라벨 해석 실패 시 멤버 후보를 재해석해 실명 복원을 강화하고, `담당 담당 미확인` 중복 문구를 제거
- 2026-03-06 - 학생관리 77차 반영: 라벨 fallback 함수의 `ev` 참조 오류를 수정해 시간표 렌더 중단(축선/일정 미표시) 현상을 긴급 복구
- 2026-03-06 - 학생관리 78차 반영: 초기 출석 동기화를 owner 전체+상태 우선순위 병합으로 보강해 `출석 -> 결석` 재역전 경로를 추가 차단
- 2026-03-06 - 학생관리 79차 반영: `getAttendanceRecordsByOwner(null)`의 현재교사 fallback 오동작을 수정해 owner 전체 조회가 정확히 동작하도록 보강
- 2026-03-06 - 학생관리 80차 반영: 묶음 일정 모달 owner fallback의 현재교사 강제치환을 제거해 legacy owner 일정 저장 정합성을 보강
- 2026-03-06 - 학생관리 81차 반영: 묶음 모달 진입 시 owner/slot 재매칭을 추가해 잘못된 시간 슬롯 저장(예: 23:30 -> 18:00) 경로를 차단
- 2026-03-06 - 학생관리 80차 반영: 자동결석 DB 비교를 owner teacher+owner 전체 병합 판단으로 확장하고, 그림자 결석 정리를 owner teacher 외 전체 대상으로 확대
- 2026-03-06 - 학생관리 75차 반영: 단건 조회의 다중 레코드 선택 규칙을 상태 우선순위 기반으로 교체하고, 메모 저장 시 결석 강제 업서트 경로를 제거해 `출석 -> 결석` 재역전 가능성을 추가 축소
- 2026-03-06 - 학생관리 81차 반영: 묶음 일정 owner/start 비교를 정규화하고 모달 기본 시간을 클릭 슬롯 우선으로 적용해 오저장 경로를 축소
- 2026-03-06 - 학생관리 82차 반영: 자동결석 판정을 수업 종료시각(classEnd) 기준으로 전환해 자정 넘김 수업 조기 결석 처리 경로를 차단
- 2026-03-01 - 학생관리 84차 반영: 일정 삭제 시 동일 시간 슬롯 출석기록(DB/로컬)을 함께 삭제하도록 단건/기간/기간별 삭제 경로를 동기화
- 2026-03-08 - 신규 노트북 로컬 실행 준비: `grading-server/.env`에 Supabase URL/Service Key 반영 및 실행 전 환경변수 점검 절차 고정
- 2026-03-08 - 신규 노트북 백엔드 기동 확인: `py -m venv` + `requirements` 설치 후 `python main.py` 실행, `/health` 200 응답 확인
- 2026-03-08 - 운영 SQL API 점검: 출석메타/학생확장/점수컬럼은 반영 확인, `expense_ledgers` 세무 컬럼 미반영(`42703`)으로 `EXPENSE_LEDGER_SETUP.sql` 실행을 최우선 후속으로 고정
- 2026-03-08 - `EXPENSE_LEDGER_SETUP.sql` 운영 반영 재확인 완료: `expense_ledgers` 세무 컬럼 조회 200 확인, SQL 미반영 이슈 해소
- 2026-03-08 - 묶음 일정 모달 접근 보정: 담당 선생님 본인인데 관리자 인증이 뜨던 분기를 owner id 정규화(`resolveKnownTeacherId`) + 보조 소유자 판별(`isScheduleOwnedByCurrentTeacher`)로 수정
- 2026-03-08 - 묶음 일정 인증 오탐 2차 보정: 슬롯 owner 재해석(`resolveScheduleOwnerTeacherId`) + 학생 담당교사 기대치(`resolveExpectedOwnerTeacherIdForStudent`)를 모달 진입 판정에 추가
- 2026-03-08 - 로그 노이즈 보정: `getAttendanceRecordByStudentAndDate`의 `maybeSingle` 분기에서 0건/복수건을 재조회로 구분해 0건은 무경고 처리, 복수건만 우선순위 선택 경고로 출력하도록 조정
- 2026-03-08 - 공휴일 API 설정 반영: 루트 `.env.local`에 `DATA_GO_KR_API_KEY`를 추가해 하드코딩 폴백 경고 없이 공휴일 API 조회가 가능하도록 구성
- 2026-03-08 - 공휴일 키 로딩 타이밍 보정: `fetchPublicHolidays`에서 `window.DATA_GO_KR_API_KEY` 외 `window.env`를 재확인하고 짧은 지연 재시도로 `.env.local` 비동기 로더 레이스를 완화
- 2026-03-08 - 중복 경고 조건 보정: `teacherId=null` 다교사 조회에서 teacher_id가 서로 다른 다건은 경고를 생략하고, 동일 teacher_id 복수건(또는 teacherId 명시 조회)만 중복 경고로 분류
- 2026-03-08 - Live Server dotfile 우회: `index.html` env 로더를 `.env.local` + `env.local` 순차 로드로 확장하고 `env.local` 파일을 추가해 공휴일 키 미설정 경고 재발을 차단
- 2026-03-14 - 학생관리 106차 반영: 출석이력의 주담당 판정을 `현재 로그인 교사 고정`에서 `날짜별 실제 수업/기록 분포 기반`으로 전환해, 날짜마다 메인 담당/툴팁 교사가 동적으로 바뀌도록 보정
- 2026-03-14 - 학생관리 107차 반영: 월 상단 담당 칩의 복수 담당 안내 문구를 `담당 선생님 : 날짜별 상이`에서 `담당 선생님 : 현재 담당선생님`으로 변경
- 2026-03-14 - 학생관리 108차 반영: 복수 담당 케이스의 담당 칩을 고정 안내 문구가 아니라 `현재 배정된 담당선생님 실명`으로 표시하도록 보정
- 2026-03-14 - 학생관리 109차 반영: 상단 담당 칩이 조회 교사별로 달라지던 문제를 해결하기 위해 기준키를 `현재 로그인 교사` 우선에서 `학생의 배정 담당교사` 우선으로 고정
- 2026-03-14 - 학생관리 110차 반영: 통합 E2E 점검을 `자동(Playwright 러너/스크립트 검증)` + `수동(실기기 운영 시나리오)` 2단계로 분리해 진행 기준을 명문화
- 2026-03-15 - 학생관리 111차 반영: `출석 -> 미처리` 전환 시 간헐적으로 발생하던 `attendance_records GET 400`을 삭제 경로/owner fallback 보정으로 재발 방지
- 2026-03-15 - 학생관리 112차 반영: `출석/지각/보강/결석 -> 미처리`가 이전 상태로 롤백되던 문제를 중복 레코드 슬롯 정리 로직으로 보정
- 2026-03-15 - 학생관리 113차 반영: UUID 기반 `attendance_records.id`를 숫자로 파싱해 발생하던 `invalid input syntax for type uuid` 오류를 record_id 타입 호환 로직으로 보정

## 학생관리 106차 - 날짜별 담당선생님 동적 판정 보정
- 상태: [x] 완료
- 구현 파일: `qr-attendance.js`
- 구현 요약:
  - `loadStudentAttendanceHistory`의 슬롯 분류 기준을 고정 주담당(`currentTeacherId`)에서 전체 슬롯 기반으로 변경
  - 날짜 단위로 교사별 schedule/record 건수와 최조 시간 우선순위를 계산해 `dayPrimaryTeacherId`를 동적 결정
  - 메인 카드는 날짜별 담당교사 슬롯 우선(일정 -> 기록 -> 기타), 같은 날짜의 나머지 슬롯은 툴팁으로 유지
  - 월 상단 담당 칩은 단일 교사면 이름 표시, 복수 교사면 안내 문구(`현재 담당선생님`)로 표시
- 검증:
  - `node --check qr-attendance.js` PASS
- 다음 단계:
  - 실기기 회귀(학생관리 86차): 동일 학생의 날짜별 담당교사 변경 케이스에서 메인/툴팁 교사가 기대대로 역전되는지 확인

## 학생관리 107차 - 담당 칩 문구 변경
- 상태: [x] 완료
- 구현 파일: `qr-attendance.js`
- 구현 요약:
  - 복수 담당 케이스의 안내 텍스트를 `담당 선생님 : 현재 담당선생님`으로 교체
- 검증:
  - `node --check qr-attendance.js` PASS
- 다음 단계:
  - 실기기 회귀(학생관리 86차) 계속: 날짜별 메인/툴팁 교사 역전 + 상단 칩 문구 노출 확인

## 학생관리 108차 - 담당 칩 실명 노출 보정
- 상태: [x] 완료
- 구현 파일: `qr-attendance.js`
- 구현 요약:
  - 복수 담당 분기에서 고정 문구를 제거하고 `normalizedPrimaryTeacherId -> primaryTeacherId` 순서로 현재 배정 담당교사 이름을 조회해 `담당 선생님 : {이름}` 형식으로 표시
  - 담당교사 이름 해석이 실패한 경우 `담당 선생님 : 담당 미확인`으로 안전 fallback
- 검증:
  - `node --check qr-attendance.js` PASS
- 다음 단계:
  - 실기기 회귀(학생관리 86차): 복수 담당 달에서도 상단 담당 칩이 고정 문구가 아닌 실명으로 노출되는지 확인

## 학생관리 109차 - 담당 칩 기준키 정합성 보정
- 상태: [x] 완료
- 구현 파일: `qr-attendance.js`
- 구현 요약:
  - `primaryTeacherId` 우선순위를 `currentTeacherId -> assignedTeacherId`에서 `assignedTeacherId -> currentTeacherId`로 변경
  - `normalizedPrimaryTeacherId` 계산에서 `normalizedCurrentTeacherId` 우선 경로를 제거해, 조회 교사가 달라도 동일 학생의 담당 칩이 동일하게 노출되도록 고정
- 검증:
  - `node --check qr-attendance.js` PASS
- 다음 단계:
  - 실기기 회귀(학생관리 86차): 서로 다른 교사 계정에서 같은 학생 모달을 열어도 상단 담당 칩이 동일한 이름으로 유지되는지 확인

## 학생관리 110차 - 수납/출석/학생 통합 E2E 점검 정리
- 상태: [x] 완료
- 실행 범위:
  - 자동 검증: `tmp-e2e-runner` Playwright 러너(`npm run test:e2e`) 실행
  - 코드 안정성: `node --check script.js`, `node --check qr-attendance.js`, `node --check js/payment.js`
  - 정적 진단: `ReadLints(script.js, qr-attendance.js, js/payment.js)`
- 결과:
  - Playwright 러너 PASS(폴링/재채점 시나리오 10개 체크 통과)
  - 핵심 도메인 스크립트 문법/린트 PASS
- 다음 단계:
  - 실데이터/권한이 필요한 운영 E2E(수납 저장-집계 반영, 출석 상태전환-툴팁, 학생 배정 변경)를 사용자 실기기로 확인

## 학생관리 111차 - 출석 미처리 전환 400 방지
- 상태: [x] 완료
- 구현 파일: `qr-attendance.js`
- 구현 요약:
  - `ensureOwnerId`에서 `currentTeacherId` fallback을 uuid 형태일 때만 허용해 `owner_user_id` 타입 불일치 400 경로를 차단
  - `updateAttendanceStatusFromHistory`의 `record_id` 경로를 보강해, 레코드를 찾은 경우 슬롯 조건 삭제 대신 `id` 직접 삭제를 우선 적용
  - `record_id` 조회 실패 시 경고 로그를 남기고 기존 슬롯 기반 삭제로 안전 fallback
- 검증:
  - `node --check qr-attendance.js` PASS
  - `ReadLints(qr-attendance.js)` PASS
- 다음 단계:
  - 실기기에서 `출석 -> 미처리` 전환 3회 반복 시 콘솔 `attendance_records ... 400` 재발 여부 확인

## 학생관리 112차 - 미처리 롤백(이전 상태 복귀) 보정
- 상태: [x] 완료
- 구현 파일: `qr-attendance.js`
- 구현 요약:
  - `updateAttendanceStatusFromHistory`의 미처리 분기에서 `record_id` 단건 삭제 후 동일 슬롯(`student/date/teacher/time`) 잔존 레코드 정리를 추가
  - `deleteAttendanceRecordsForHistorySlot`에서 `owner_user_id` 필터를 선택 적용(있으면 사용)으로 변경해 owner 복구 지연 시 삭제 스킵을 완화
  - 슬롯 삭제 함수에 `attendance_date` 유효성 가드를 추가해 비정상 날짜 필터 요청을 사전 차단
- 검증:
  - `node --check qr-attendance.js` PASS
  - `ReadLints(qr-attendance.js)` PASS
- 다음 단계:
  - 실기기에서 `출석/지각/보강/결석 -> 미처리` 반복 전환 시 값이 다시 이전 상태로 되돌아가지 않는지 확인

## 학생관리 113차 - record_id UUID 파싱 오류 보정
- 상태: [x] 완료
- 구현 파일: `qr-attendance.js`
- 구현 요약:
  - `updateAttendanceStatusFromHistory`에서 `recordIdHint`를 `parseInt`로 강제 변환하던 로직을 제거하고, 숫자/UUID 모두 허용하는 문자열 검증으로 교체
  - `deleteAttendanceRecordById`에서 UUID를 숫자로 잘라쓰던 변환(`parseInt`)을 제거하고 원본 id 문자열로 삭제하도록 변경
- 검증:
  - `node --check qr-attendance.js` PASS
  - `ReadLints(qr-attendance.js)` PASS
- 다음 단계:
  - 실기기에서 `출석/지각/보강/결석 -> 미처리` 전환 시 콘솔의 `invalid input syntax for type uuid` 재발 여부 확인
