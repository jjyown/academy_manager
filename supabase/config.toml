# PIN 검증은 함수 내부에서 service role로 처리. 게이트웨이 JWT 검증이 켜져 있으면
# 세션 만료/비정상 시 401만 보이고 본문이 비는 경우가 많아 클라이언트 혼선을 줄이기 위해 false.
[functions.verify-teacher-pin]
verify_jwt = false

[functions.upload-homework]
verify_jwt = false

[functions.send-reset-code]
verify_jwt = false

[functions.exchange-google-token]
verify_jwt = false

# 종합평가 AI: 게이트웨이 JWT 검증이 켜져 있으면 세션 타이밍에 401만 나오고 본문이 비는 경우가 많음.
# 실제 권한은 함수 내부에서 getUser() + students.owner_user_id 일치로 검증.
[functions.generate-student-eval-report]
verify_jwt = false

# 입시 정보 수집: 함수 내부에서 Bearer 세션 또는 x-cron-secret 으로 인증.
# 게이트웨이 JWT 를 켜두면 cron 호출(서비스롤+x-cron-secret) 이 막혀 비활성화.
[functions.collect-admissions-knowledge]
verify_jwt = false
