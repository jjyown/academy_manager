# 환경 변수 (보안)
.env
.env.local
.env*.local
grading-server/.env

# MCP 설정 (PAT 평문 포함)
.mcp.json

# 빌드
dist/
build/
.next/
out/

# 의존성
node_modules/
package-lock.json
yarn.lock

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# 로그
npm-debug.log
yarn-debug.log
yarn-error.log

# Vercel
.vercel/

.venv/
grading-server/.venv/
__pycache__/

# 자동 생성된 cron 적용 SQL (CRON_SECRET 포함 — 절대 커밋 금지)
migrations/_GENERATED_*.sql

# 옛 런타임 검증 산출물(대용량 JSON) — 코드/검증 스크립트만 추적, 결과 JSON은 로컬용
qa-artifacts/runtime-*.json
qa-artifacts/report.json
qa-artifacts/quick-recheck-report.json

# Supabase CLI 로컬 캐시(연결 프로젝트 정보 등 — 사용자별 상태)
supabase/.temp/
